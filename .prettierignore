/dist
/coverage
# openapi-generator output — regenerated by `pnpm generate:api`, never edited.
apps/ui/src/renderer/autogenerated
# The pinned ACP schema release, vendored byte-for-byte from its GitHub release
# asset. Its spec asserts the file's sha256, so reformatting it here would fail
# the suite rather than merely churn the diff.
apps/daemon/src/v1/agents/adapters/acp/__tests__/acp-schema.*.json
