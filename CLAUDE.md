# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## Project Overview

**geniro-app** is a **local-first macOS desktop app** for composing and running a **DAG of CLI coding agents** as a team. It is a from-scratch rewrite of Geniro that marries Geniro's graph engine with a local-first, CLI-agent execution layer — **everything runs on the user's machine, no cloud**.

The app is an **Electron UI** that also supervises a **bundled local daemon** over loopback. The daemon is an `apps/api`-style **NestJS** app built on packages **vendored from the Geniro monorepo** and adapted for local-first use (SQLite instead of Postgres, no Sentry/Redis/cloud, loopback-only).

**Tech stack**: TypeScript 6.x, Node.js 24+, NestJS 11 (Fastify), MikroORM (`@mikro-orm/sqlite` / better-sqlite3), React 19 + Tailwind CSS v4 (electron-vite renderer), pnpm 11 + Turbo 2.10 monorepo, swc (daemon + packages) / electron-vite (UI), Vitest 4, ESLint 10 + Prettier 3.

**Status**: **All four v1 milestones are built** — M1 (UI + infrastructure), M2 (single-agent chat via CLI adapters), M3 (workflow graphs + DAG fan-out execution), M4 (CLI handoff, Settings, update notifier, macOS packaging). The plan and milestones live in `.geniro/planning/geniro-app-v1/` (`spec.md` + `milestone-1..4.md`) — this is the authoritative source for scope and sequencing. (`.geniro/planning/` is local working state, gitignored — not committed.)

**Agents (M2+)** are driven **headlessly via their CLIs only** — `claude -p`, and `cursor-agent` over its first-party **ACP** server (`cursor-agent acp`). No SDKs, no LangGraph host-side, no Python.

---

## CodeGraph — prefer it over grep/find

This repo is indexed by **CodeGraph** (a `.codegraph/` directory exists at the repo root — it is gitignored and **not committed**, but it is always present locally and must be kept in use). When you need to understand or locate code, reach for CodeGraph **before** grep/find or opening files:

- **MCP tool** (preferred): `codegraph_explore` answers most code questions in one call — the relevant symbols' verbatim, line-numbered source plus the call paths between them, including dynamic-dispatch hops grep can't follow. Name a file or symbol in the query to read its current source.
- **Shell** (always works): `codegraph explore "<symbol names or question>"` prints the same output.

Fall back to grep/Glob/Read only when CodeGraph has no answer (e.g. non-code files, or code still missing after the sync below).

**`codegraph sync` first in any worktree — "not yet indexed" is a 15-second fix, not a reason to reach for grep.** Only the checkout holding `.codegraph/daemon.sock` is kept warm; a worktree has no daemon, so its index freezes at creation and drifts for days. `codegraph status` does **not** reliably tell you: measured 2026-08-13 (v1.1.1) on a sibling monorepo, it printed `✓ Index is up to date` on two worktrees where `sync` then found 1,087 and 629 unindexed files. A stale index answers a real symbol with `No results found` — indistinguishable from "doesn't exist" — and `codegraph_explore` fills the hole with token-level matches on unrelated code under a banner promising current on-disk source.

```bash
codegraph sync                 # 12–23s incremental; the only authoritative staleness check
```

One sync per worktree per session is enough **until you edit files** — nothing re-syncs automatically, so a file written after it stays invisible (and a deleted file's symbols linger) until the next sync. Do not use `init`/`index` for freshness: those are full rebuilds costing ~580 MB per checkout.

---

## Commands

All commands run from the **repo root** unless noted. **Always run `pnpm install` first** to ensure dependencies are present.

### Daily development
```bash
pnpm install            # install workspace deps
pnpm rebuild:native     # rebuild better-sqlite3 against Electron's ABI (required; see note)
pnpm build              # build everything (turbo → swc for packages+daemon, electron-vite for the UI)
pnpm dev                # launch the Electron app (electron-vite) — spawns + supervises the daemon
pnpm daemon:dev         # daemon-only watch loop: runs TS source via @swc-node/register under Electron-node, restarts on save
```

`pnpm rebuild:native` is required because the daemon runs under Electron's bundled Node (`ELECTRON_RUN_AS_NODE`), so its native `better-sqlite3` must be built for Electron's ABI, not the host Node ABI.

`pnpm daemon:dev` mirrors Geniro `apps/api`'s `start:dev`: node's built-in `--watch` + `-r @swc-node/register -r tsconfig-paths/register` running `src/main.ts` directly (`TS_NODE_PROJECT` points at the **root** tsconfig so the inherited `@packages/*` paths resolve from the repo root). No build step and no `dist/` in dev — `@packages/*` resolve to TypeScript **source**, so edits to `apps/daemon/src` *and* `packages/*/src` restart the daemon (~2s) with fresh code. Two deliberate deviations from Geniro's line: the runtime is `ELECTRON_RUN_AS_NODE=1 electron` (host Node's ABI can't load the Electron-built `better-sqlite3`), and there is no `--watch-kill-signal=SIGKILL` (default SIGTERM lets Nest shutdown hooks run — pidfile cleanup + `ProcessRegistry` reaping of spawned agent children). It must **not** be moved to `tsx`/esbuild: esbuild's `emitDecoratorMetadata` is an intentional wontfix (no type system → no `design:paramtypes`), so NestJS type-based DI cannot resolve under it.

### Build, types, lint
```bash
pnpm build              # turbo run build (swc → dist/ for packages & daemon; electron-vite → out/ for the UI)
pnpm check-types        # turbo run check-types (tsc --noEmit per package — swc does NOT type-check)
pnpm lint               # eslint, no fixes
pnpm lint:fix           # eslint + prettier auto-fix
```

### Testing
Always use the package.json scripts — never call `vitest` directly.
```bash
pnpm test:unit                       # all unit tests (vitest, *.spec.{ts,tsx})
pnpm --filter @geniro/daemon test:unit     # just the daemon
pnpm --filter @geniro/ui test:unit         # the UI
```

### Before finishing any work
```bash
pnpm full-check         # must pass before finishing
```

`full-check` chains build → check-types → build:tests → lint:fix → test:unit → test:integration. `build:tests` and `test:integration` are **no-op placeholders today** — declared in `turbo.json` (so turbo 2.10 doesn't error on them) but no package implements them yet and no `*.int.ts` exist — so it effectively gates build + types + lint + unit tests until integration tests land.

### Packaging (macOS)
```bash
pnpm --filter @geniro/ui build:mac   # scripts/build-mac.mjs → DMG + zip into release/dist/
```

### Regenerating the renderer's daemon client
```bash
pnpm generate:api       # daemon OpenAPI → apps/ui/src/renderer/autogenerated/ (committed)
```
Run after changing any daemon route or wire schema. Needs the daemon BUILT
(`pnpm build`) — it always boots a throwaway daemon on a temp userData dir,
NEVER reading one that happens to be running (which would describe an older
build and silently regenerate a client missing the route you just added).
Requires a JVM (openapi-generator-cli), same as the sibling repo.

### Dependency upgrades & commits
```bash
pnpm upgrade            # bump every workspace dep to latest (ncu, peer-aware via .ncurc) + reinstall
pnpm commit             # conventional commit via commitizen
```

> Database: the daemon **syncs the SQLite schema additively on launch** (`orm.schema.update({ safe: true })` in `main.ts` — never destructive). There is still **no Migrator / migration files**: `db/mikro-orm.config.ts` declares no `migrations` key, and the versioned migration workflow remains deferred past v1. Do not hand-write migrations.

---

## Architecture

A **pnpm + Turbo monorepo** whose root config and server packages are **cloned from the Geniro monorepo** and adapted for local-first use.

```
apps/
  ui/               @geniro/ui       — Electron main + preload + React renderer (electron-vite)
  daemon/           @geniro/daemon   — NestJS loopback daemon (apps/api-style) over @packages/http-server + mikro-orm SQLite
packages/
  common/           @packages/common — AppBootstrapper, pino logger, exceptions (vendored from Geniro; Sentry removed)
  http-server/      @packages/http-server — NestJS + Fastify host: health, swagger/scalar, helmet, validation, jose (vendored; OIDC auth dormant; + loopback listen opts host/portFallback/onListening; + utils/openapi-schemas — canonicalizes the per-direction duplicate components nestjs-zod emits, and fails the boot on a dangling $ref)
  metrics/          @packages/metrics — Prometheus metrics (vendored from Geniro)
  mikroorm/         @packages/mikroorm — TimestampsEntity base, BaseDao, MikroOrmModule (vendored; driver swapped to @mikro-orm/sqlite)
```

### The daemon (`apps/daemon`)
A standalone NestJS engine the UI spawns as a child process, laid out like Geniro's `apps/api/src`: `main.ts` (bootstrapper + extensions → `init()`) and `app.module.ts` at the root, then feature/infra dirs:
- `auth/` — `runtime.ts` (per-launch `RuntimeInfo` + DI token, incl. the bound `port`) + global `runtime.module.ts` (also hosts `CallTokenRegistry`, so the guard and the graph executor share it without a module cycle); `mint-token.ts` (the one source of loopback credentials — the launch token AND the per-caller-node MCP call tokens); `token.guard.ts` + `safe-equal.ts` (loopback bearer-token gate); `ws-auth.ts` (`enforceWsHandshakeAuth` — the ONE Socket.IO handshake gate, shared by every gateway, since engine.io bypasses Nest guards); `call-token.registry.ts` (per-`(runId, nodeId)` MCP call tokens).
- `environments/` — `environment.{prod,dev,test}.ts` + `index.ts` (picks by `NODE_ENV`, loads `.env` via dotenv), laid out like the sibling api's: prod is the base, dev spreads prod, **test spreads DEV**, and each is one object literal closed with `as const satisfies Record<string, string | number | boolean | null>` — the shape is INFERRED, so there is no hand-written interface to keep in step and a new field is typed at every read site without a second edit. An env file holds environments and nothing else: `readDaemonVersion` lives in `utils/daemon-version.ts`, and the strict parsers in `utils/handshake.ts` beside the rest of the UI→daemon contract. Three fields deliberately bypass `getEnv` and say so at their own line — it boolean-coerces `'0'`/`'1'`/`'on'`/`'off'`, which is wrong for `GENIRO_USER_DATA` (a path), `GENIRO_PORT` (must be a bindable 1..65535) and `GENIRO_IDLE_EXIT_MS` (malformed ⇒ feature OFF, never a guessed number). `host` is a CONSTANT rather than an env knob — the one deliberate departure from the sibling's env-everything shape, because "binds 127.0.0.1 only" is a hard v1 constraint and an env var is how it would come to be broken.
- `utils/` — `handshake.ts` (pidfile `DaemonInfo` shape + loopback bind defaults + `isValidPort`/`parsePort`); `pidfile.ts` (write/remove — `mintToken` moved to `auth/mint-token.ts`); `pidfile.lifecycle.ts` (Nest `OnApplicationShutdown` → removes the pidfile); `atomic-file.ts` (stage-then-rename writes: `atomicWrite`, its `Sync` sibling for callers that cannot yield, and `atomicCreate`, the exclusive form the instance lock claims with); `process-identity.ts` — the ONE answer to "is pid N still the process I recorded", `ps`-reported start time within a 2s tolerance, shared by the two things that act on a pid they do not hold a handle to (a bare `kill(pid, 0)` cannot tell a recycled pid from the original, and both readers below would then SIGKILL or refuse to boot on a stranger); `instance-lock.ts` + `instance-lock.lifecycle.ts` — **one daemon per userData dir**. The port fallback stays (a FOREIGN process on 47615 must not block a start), but a second GENIRO daemon is refused: it would share the SQLite file and overwrite the pidfile, which is exactly what a stray `pnpm daemon:dev` did for 19 hours beside the app's own daemon. The lock records the holder's process start time, so a SIGKILLed daemon's leftover lock is self-healing with no staleness window to tune — the next launch cannot confirm a dead holder and takes over at once. Released on Nest's shutdown hook, NOT `process.on('exit')`: Nest re-raises the signal after its hooks and default signal termination never runs `exit` listeners (observed — the pidfile was cleared while the lock file was not); `idle-shutdown.lifecycle.ts` — the daemon exits ITSELF after `GENIRO_IDLE_EXIT_MS` with no connected WS client **and** no in-flight turn. Two conditions, not one: no client is what makes it unused, no turn is what makes it safe to stop, and a workflow keeps running after its window closes. It exists for the launches the UI supervisor can never clean up after — it only terminates the daemon it OWNS, and a force-quit or crash leaves one it does not. SIGTERM at itself rather than a direct exit, so the `ProcessRegistry` drain and pidfile cleanup still run. **Disabled unless the environment names a window**, which only `DaemonSupervisor.spawnDaemon` does (10 min): `pnpm daemon:dev` and the throwaway `pnpm generate:api` daemon have no client by design and must not read that as being unwanted; `crash-guards.ts` — the process-level `unhandledRejection`/`uncaughtException` handlers, installed before Nest so a stray rejection logs instead of crashing past the `ProcessRegistry` drain. An uncaught exception SIGTERMs the daemon at ITSELF so the shutdown hooks still run, and that is the one thing here with a trap in it: **Nest runs those hooks inside its own SIGTERM listener**, so a hook that throws comes straight back as another uncaught exception. It is latched (`shuttingDown`) for exactly that reason — unlatched it sent another SIGTERM and ran the hooks again, at ~80,000 re-entries per second (measured against this module's own built output), each arming a 6.5s failsafe timer that cannot have fired yet, so half a million live timers and their closures piled up until V8 aborted. That abort is the macOS **"Electron quit unexpectedly"** dialog a user reported seeing periodically: two crash reports, `node::OOMErrorHandler` in both, and the live process caught at 82% CPU / 3.4GB with a main thread of `uv__signal_event` → JS → `TriggerUncaughtException` → JS. Every handler body is also wrapped so it cannot throw out of itself — the debug sink writes to a file, and a stream the shutdown already closed is the same loop reached through the logger.
- `db/mikro-orm.config.ts`.
- `v1/runs/` — `runs.module.ts` + `entity/{run,item,node-state,run-group}.entity.ts` + `runs.types.ts` (status/kind enums). `run_groups` is the sidebar's own folders — runtime state, not a definition, so it sits in SQLite beside the runs it holds rather than in YAML (which is reserved for the workflows a user authors and exports).
- `v1/agents/` — the shared agent-execution substrate (M2): `agents.module.ts` + `chat.types.ts` at the root, then `adapters/`, `controllers/`, `services/`, `dao/`, `dto/`, `utils/`.
  - `adapters/` — the abstract `AgentAdapter` base + `adapter.types.ts` (the CLI-agnostic contract, incl. `AdapterConfig`/`AdapterQuestion`/`TurnDriver`/`HandoffResult`) + `utils/skill-scan.utils.ts` (the shared on-disk walk, free of any one CLI's paths), then one directory per PROTOCOL several CLIs could speak and one per CLI — see `.claude/rules/agent-adapters.md`. **Every static fact about a CLI is one `AdapterConfig`**, assembled by that adapter's own `getConfig()`, so `listSkills`, `listEfforts`, `listReportedCommands`, `supportsLiveStream`, `resolveApprovalMode`, `handoffTarget`, `loginTarget` and `errorRecovery` are CONCRETE on the base over config; only `getConfig`, `buildArgs`, `mapMessage` and `listModels` are abstract. The `auth` block sits beside `mcp` and `configDir`: that CLI's own sign-in argv, the reason it has none, and the wording that marks a failed turn as a lapsed ACCOUNT session (evidence-gated — cursor declares `[]` because no such failure has been observed from it, so no row offers a cure invented from `--help`).
    - `acp/` — the agent-agnostic **Agent Client Protocol** client: `acp.types.ts` (wire shapes), `acp-jsonrpc.ts` (framing), `acp-content.ts` (attachments → `image` content blocks), `acp-driver.ts` (the per-turn state machine). It knows nothing about cursor; a second ACP-capable CLI composes it rather than copying the protocol.
    - `claude/` — `claude.adapter.ts`, `claude.const.ts` (ALL of that CLI's constants), `claude.types.ts`, `claude-probe.service.ts` (the `--permission-mode` support probe), and `utils/` (`claude-message` — `mapClaudeMessage` + the two stream-partial mappers; `claude-usage`; `claude-models` — the account models the CLI caches in `~/.claude.json`; `claude-images` — base64 content blocks; `claude-question` — defensive projections over an AskUserQuestion input: question text, option labels, and `withResponse`, which picks the answer channel by question COUNT — `updatedInput.answers` (a map keyed by question text) for a lone question, `updatedInput.response` (one free-text reply, which REPLACES the structured list) for several, the choice probe-verified on 2.1.226; `claude-mcp-config` — the per-turn 0600 `--mcp-config` and `--settings` files, their boot sweeps, and the geniro-key collision check; `claude-mcp-folder` — what claude's OWN config files say about a folder (which servers are project-scope, which the user disabled themselves); `claude-mcp-list` — `parseMcpList`, the prose `claude mcp list` output turned into rows; `claude-mcp-ready` — the `mcp_status` request line and the reply reader behind the readiness gate below; `claude-context-usage` — the SECOND undocumented control request this adapter drives, `get_context_usage`, found the same way as `mcp_status` and probed live on 2.1.232: the wire behind that CLI's own `/context`. The reader projects the ~80KB reply down to what a readout can use, and three measured facts shape it — an `isDeferred` row is OUTSIDE `totalTokens` (verified by arithmetic: the non-deferred rows sum exactly to it), `Free space` is the remainder rather than a category and is dropped so nothing double-counts it, and the 371 per-tool rows are folded into one per SERVER because the actionable fact (one server holding 109k of the 274k) is invisible until they are summed; `claude-tasks` — that CLI's own task list read off BOTH directions of a tool call, since the `TaskCreate` id is only in the result and the `TaskUpdate` status only in the input), plus `claude-turn.driver.ts` — that gate.

      **A claude session's FIRST prompt is held until the CLI's MCP servers have finished dialling.** The report was "I have playwright in mcp servers list … But agwnt cant use it", with a screenshot of geniro's panel showing `playwright` green beside the agent in the same folder replying that the server was not loaded in its session — and both were honest. Measured on 2.1.232 in the author's own 45-server folder: the CLI dials its servers when the PROCESS starts and does NOT wait for them before running a turn, so a prompt written at 0s got an init at 3.1s with 8 servers still `pending`, 148 tools and none of playwright's, while the same prompt held to 8s got 0 pending, 466 tools and all 24. Nothing is permanently missing — the surface GROWS mid-turn (a server `pending` at init answered a tool call 29s later) — so the damage is entirely in what the model concludes: it asks once, at second three, is told the tool does not exist, and works around a capability it actually has for the rest of the conversation. That is also why it is intermittent. The oracle is `mcp_status`, an undocumented client-initiated control request on the same stdin dialogue as `can_use_tool`, found by enumerating the shipped binary's subtypes and then driven live: it answers BEFORE any prompt has been written, with one row per server under `mcpServers` (not `servers` — reading the obvious name returns an empty list from every healthy reply, which is indistinguishable from "discovery has not started"). The gate polls it and releases on two IDENTICAL readings with nothing `pending`, never one: discovery is gradual (6 servers at 0.8s, 45 at 1.2s, and a `dynamic`-scope row present in the first reading absent from the second), so a single quiet moment is also what a half-discovered list looks like. Only `pending` blocks — waiting for `needs-auth` to become `connected` would hold every first message for the full deadline on any machine with one signed-out server. Three exits bound it: an empty reading is believed for a 2s grace (a machine with no servers), a `refused` reply releases at once (a renamed subtype costs the fix and never the turn), and a 15s deadline releases with a `notice` NAMING the servers still starting, because a turn that silently runs on the old surface is the bug report again. The seam is agent-agnostic — `TurnDriver.awaitPromptReady` awaited by `CliTurnOptions.holdPrompt` in `spawn-cli.ts`, FIRST turn only (the wait belongs to the process, and by turn two the servers have been up for a whole turn) — and cursor defines neither, so its opening write stays synchronous. Two claude turns are outside it as well: one carrying `isolateMcpServers` loads no server by construction, and one naming no approval mode is a geniro-internal probe that never reaches a tool. A cancel during the hold DROPS the prompt rather than writing it into a turn the user already stopped, and a gate that throws is logged and the prompt sent anyway — losing the user's message would be worse than the defect being fixed.
    - `cursor-acp/` — `cursor-acp.adapter.ts` (binary name, env re-injection, the approval policy, the session-mode mapping, and a `getConfig()` whose single-reader facts are written INLINE), `cursor-acp.const.ts` (the values that are NOT single-reader: the `mcp list` argv and the probed row markers its parser and its adapter both spell) and `utils/` (`cursor-mcp-list.utils.ts` — `parseCursorMcpList`; `cursor-todos.utils.ts` — `parseCursorTodos`, the `cursor/update_todos` payload; `cursor-context-store.utils.ts` — **that CLI's own context accounting, read off disk**). It still has no `.types.ts` — the wire shapes live in `acp/`. Its `mapMessage` throws by construction: an ACP adapter has no line mapper, `createTurnDriver` returns an `AcpTurnDriver`, and `acp/acp-driver.spec.ts` is what drives the protocol without a process.
  - `services/` — chat service, event bus, process registry, approval registry, adapter registry, attachment store, partial-stream; `agent-session.registry` — the run-scoped CLI processes, one per run, kept BETWEEN turns so a chat's MCP servers boot once instead of once per message. It is deliberately separate from `ProcessRegistry`, which tracks TURNS: a turn now ends while its process keeps running, so "is this run busy" and "does this run hold a process" stopped being the same question. Bounded by `MAX_LIVE_SESSIONS` with an LRU eviction over IDLE entries, an idle window, and a `session.closed` subscription — a dead session reports `idle === false`, so without that subscription it would occupy a slot forever and get a LIVE session evicted in its place; `harvest-store` + its `mcp-harvest`/`skill-harvest` subclasses — what a CLI reported about a folder on turns that actually ran there, so the panel need not re-dial from cold. The MCP one is age-bounded because it SHADOWS the live listing rather than merely caching it (consulted above the CLI ask, so an unbounded harvest makes the lapsed-TTL re-dial unreachable); the skills one is not, because it is MERGED with the other sources; `models`/`efforts` (the vocabularies one CLI reports about itself, cached per `--version` + TTL); `agent-mcp` — the MCP servers a CLI loads in one folder, asked of the CLI itself (`AgentAdapter.listMcpServers`) and cached per `(agent, cwd, configDir, version)` + TTL — the config directory belongs in the key because a profile carries its own servers, so two nodes with different ones genuinely load different sets, and the listing itself is READ under that profile's env for the same reason — with a keyed single-flight because the read HEALTH-CHECKS and so launches the user's own servers; `skills` — the composer's `/` autocomplete, a CLI-agnostic composition of three sources: the adapter's own disk scan (`listSkills`), the per-(agent, cwd) `skill-harvest` store — the `slash_commands` a CLI reported on turns that actually ran in that folder (claude's `system/init`, cursor's ACP `available_commands_update`), keyed by `(agent, cwd)` so one folder's two agents never serve each other's commands, cached in `<userData>/claude-skills.json` — and the adapter's self-report (`listReportedCommands`), which is what makes a folder no turn has ever run in list the CLI's built-ins instead of nothing; `chat-metrics` — the composer's context readout: the breakdown (`AgentAdapter.readContextUsage`, an adapter fact) beside the thread's spend summed from its own `turn_complete` rows, with the reason for an absent breakdown coming from that adapter's own `usage.breakdownUnavailableReason` rather than from a branch here. It composes only: it hands the adapter BOTH channels a CLI could answer on — `{live, sessionId}`, the run's kept session if it holds one and the id its `node_state` recorded — and each adapter takes what its own CLI has. That shape is what a claude-only seam could not express: claude answers over the live process's stdin, cursor holds no process between turns and answers off its own on-disk session store, so a seam built around a live session left the CLI with the readable figures unable to report any; `run-teardown` — the ONE delete teardown (cancel → wait for the caller's in-flight work to stop writing → revoke call tokens, purge `runs`/`items`/`node_state` with the `softDelete` filter disabled, drop attachments, announce `runDeleted`), shared by the chat delete and the graph executor's `deleteRun` so neither can drift out of clearing a store.
  - `utils/` — json-util, ndjson-buffer, spawn-cli (+ child-env, kill-tree; its `CliSession.ask` is the out-of-band question seam — write one line to a live CLI's stdin and watch parsed stdout for the reply, bounded by the caller's own deadline. Additive by construction and that is the safety claim: the reader is offered each object BEFORE the mapper and neither can consume the other's lines — a control reply maps to no event, an event line answers no ask — so a readout can be opened mid-turn without perturbing it, which is measured (three concurrent asks during a 45s tool call, the turn completing with its full output). Agent-agnostic: the reader owns the correlation, so this module never learns a CLI's control vocabulary), event-to-item, persist-item, resolve-cwd, resolve-config-dir, resolve-directory (the shared validate-and-canonicalize the other two are built on), skill-markdown, approval-answer, message-preview, session-saver, run-kind/run-status, unanswerable, ws-payload; `agent-binary` + `agent-version` (the `--version` probe-cache key); `child-handle` — wraps short-lived utility children as registrable handles. **Nothing here is named after a CLI** — a helper that would be belongs in that CLI's own `adapters/<name>/utils/`.
    **A turn does not end on its CLI's turn-end line while background work it started is still out** (`spawn-cli.ts`, `TurnState.openWork`/`deferredTerminal`, fed by the agent-agnostic `background_work` event). The turn-end line is the end of what the agent was SAYING, never of what its process is DOING: probed on claude 2.1.231, a turn told to launch a delegate and not wait for it printed its `result` while the delegate was still running, and the CLI then ran a whole FURTHER turn of its own accord as the delegate reported — that continuation's own result line says so, `origin:{kind:"task-notification"}`. Settling on the first `result` therefore ends geniro's turn mid-work and hands everything after it to nobody, which the author's own daemon log measures at 11 of 31 settles followed by off-turn work, up to 33 minutes and 997 events past the settle, 227 whole assistant messages dropped for want of a turn to put them in and 430 permission requests answered with no card ever shown — while the run reported `completed` and its delegates rendered `stopped`. So a `turn_complete` is HELD while `openWork` is non-empty and released when the last unit reports; the bound is the turn's existing silence deadline, which releases the held result rather than overwriting a finished turn with "produced nothing…". Only a `turn_complete`, and only on a `session` stdin lifetime: a cancel is the user asking to stop now, an `error` is a turn that failed rather than finished, and a turn whose process ends with it has no "after" to wait for. Claude reports the lifecycle on `system/task_started` + `task_updated`/`task_notification` (both terminal channels mapped — they spell one outcome `killed` and `stopped`); a CLI that reports none simply never emits the event and its turns settle exactly as before, so this is not a capability an adapter must declare.

    **What the CLI does AFTER its turn settles belongs to the run, not to nobody** (`ChatService.handleBetweenTurnEvent`). A turn's `result` line ends the turn geniro started; it does not stop the CLI, which routinely opens a further turn of its own accord when a delegate reports back — that continuation's own result names itself `origin:{kind:"task-notification"}`. `spawn-cli` has no turn to hand that output to, so it arrives on `onBetweenTurnEvent`, which used to keep only the two halves of a tool call on the argument that those carry an id pairing them while a stray message has no anchor. Measured on a live delegating chat (2026-08-14, claude 2.1.232): seven seconds after the turn settled the CLI ran a whole further turn, and that filter dropped 2 `text` events, 5 `text_delta`, 2 `turn_complete`, a `session` and every progress and harvest event, keeping only the tool row. What the user sees is what was reported — a run badged `completed` with more work appearing under it, and the agent's own messages after that point simply not arriving. The rule is now the in-turn rule: whatever `mapEventToItem` yields a row for is persisted in arrival order, the live signals feed the same partial stream, the self-reports feed the same harvest stores, and the continuation's own terminal event settles the run again. A settled run producing rows goes back to `running` with `offTurnActivity` naming it as the CLI carrying on by itself rather than as a fresh turn. Two runs are exempt: one the user CANCELLED (Stop is final — a straggling result flipping `cancelled` back to `completed` says their Stop did not take, though the rows are still written, because a transcript that hides work is lying the other way), and one whose `running` this path did not write, which `offTurnRuns` is what distinguishes — the handler is async, so a real turn can have opened in between, and settling THAT turn's run on the previous one's tail is the defect `cancelledTurnMayStillEmit` exists to stop. Verified on the same scenario after the fix: zero dropped events, and two whole assistant messages plus their costs recovered into the transcript.

    **That silence deadline does not run at all while the turn is blocked on a verdict** (`TurnState.outstanding` non-empty), and that is the absence of a window rather than a longer one. The deadline's premise is that silence is UNEXPLAINED — it exists to catch a CLI nobody can account for. A turn parked on an approval card is the opposite: geniro raised the request, knows the CLI is blocked on it, and is itself the thing withholding the answer, so the clock would be timing a HUMAN, and a human who steps away for lunch has not wedged. Measured on the user's own daemon log (2026-08-11, two chats): an `AskUserQuestion` card went up at 16:30:40 and nothing arrived after it because nothing could; 30 minutes and one second later the turn was settled as `error`, the run marked failed, and the card the user was coming back to answer rewritten as `unanswerable` — a sentence false in every clause, since the CLI had produced both an answer and a question and had given up on nothing. `respondApproval` rearms on DELIVERY, and from that moment rather than from when the card went up, so an answer at minute 29 does not leave the turn a minute from being abandoned; a second still-open card keeps it suspended. Two consequences are deliberate: Stop is then the only bound on a parked turn (which is correct — it is waiting for its user, who is the only one who can end the wait), and a parked turn keeps `ProcessRegistry.activeCount` non-zero, so `IdleShutdownLifecycle` will not fire while a question is on screen.

    **A KILL is not a cancellation, and only `cancelRequested` can tell them apart** (`settleFromTermination`'s signal branch). A child that exits on a signal used to settle `turn_cancelled` unconditionally, which is right for the terminations geniro ASKS for — Stop and the shutdown reap both reach `cancel()`, a retired session reaches `close()`, and both set the flag — and wrong for every other way a process dies: an OOM kill, a crash, a `pkill`, a supervisor taking the group down. The damage is not the word but what the word suppresses. `cancelled` is deliberately the ONE ending the app never notifies about (announcing a Stop tells the user what they just did), so a killed agent settled quietly under a badge saying the user had stopped it — a run that failed, told nobody, and blamed them for it. Measured by SIGKILLing a live `cursor-agent acp` mid-turn: before, the run read `cancelled` with no banner; after, `failed` with `The turn failed.` posted. So the two halves of "errors handling" had one cause, and the notification rules never needed touching — they had been correct about `failed` all along and were simply never handed one. The signal is NAMED in the error message (`… was terminated by SIGKILL`), with the stderr tail beside it, since "the process ended" is not a diagnosis.
    **The agent's OWN task list is a transcript row, not a tool call** (`task_list` items; `AgentEvent`'s `task_list`, `adapters/claude/utils/claude-tasks.utils.ts`, `adapters/cursor-acp/utils/cursor-todos.utils.ts`, the agent-agnostic `AcpTodoProtocol` seam in `acp-driver.ts`). Every shipped CLI keeps a checklist while it works a multi-step job, and geniro showed neither the list nor its progress: the report was "это отображается просто как вызов тулов, а текущий статус вообще не отображается" — and that was literally true of both CLIs. Measured 2026-08-14 on claude 2.1.232, a three-step job produced nine anonymous `Used 1 tool` rows and nothing else; on cursor-agent 2026.08.11-e8db854 the same job produced a `Update TODOs` row that discloses NO arguments (`rawInput:{_toolName:"updateTodos"}`), so the list existed nowhere in the app. **`mode` is the load-bearing field**: two of the three shapes on the wire are PATCHES naming only the rows that moved (claude's `TaskUpdate` sends `{taskId,status}` and no text at all; cursor sent three `merge:true` frames carrying two of its three rows), so a consumer reading any announcement as the list shows one task where there are seven. It is never inferred from the contents — a patch that happens to name every row is indistinguishable from a snapshot — the CLI says which it sent, and cursor says so literally. Absent, it reads as a patch: the direction that leaves a stale row rather than deleting every task it did not name. **Claude's half is read off BOTH directions of a tool call**, which the CLI forces rather than geniro choosing it: a created task's id exists only in the result (`Task #1 created successfully: …`), an update's status only in the input, and `TaskList`'s result is the one place the whole list is stated — so the result parsers match the CLI's own anchored sentences, a `tool_result` block carrying `tool_use_id` and no tool name. There is no pull channel to ask instead: the 2.1.232 control subtypes were enumerated the way `mcp_status` was found, and the two task-shaped ones answer other questions (`background_tasks` takes a `tool_use_id`, `get_plan` returns the plan-mode file); the push channel that DOES carry the full list (`system/background_tasks_changed`) goes to the CLI's remote-control bridge and never appeared on stdout across a five-task probe. **Cursor's half was a vendor extension being declined in the quietest possible way** — `cursor/update_todos` is on `CURSOR_SILENTLY_DECLINED_METHODS`, so it was refused in-protocol with the transcript notice deliberately withheld; the third such method found to be REAL after `cursor/ask_question` and `cursor/task`, and the seam is agent-agnostic so a fourth CLI's channel is a config rather than a branch. Baseline ACP's `plan`/`plan_update` updates are NOT what either agent sends. The event carries the producing `toolCallId` so the renderer shows the list INSTEAD of that opaque row, and it is a DURABLE row rather than a live signal because the list is only ever the fold of its announcements — a reopened chat has to replay them all. A delegate's list is its own: the announcement carries whatever origin its line carried, so nothing about tasks knows about sub-agents. That path is measured but unproven on the wire — told explicitly to keep one, a claude delegate searched its own toolset three times and found no task tools, so the CLI does not hand them to a delegate.

  - Exported to the graphs module: event bus, approval registry, partial-stream, `ClaudeProbeService`, process registry, `RunTeardownService`, `SkillHarvestStore`, the three DAOs, both adapters, and the adapter registry.
- `v1/graphs/` — the M3 workflow module + the agent-to-agent **call runtime**: `graphs.module.ts` + `graphs.types.ts` (zod Workflow/node/edge/layout schemas + the call-runtime contract: `CallMode`/`CallEnvelope` (incl. the M4 `question` arm)/`CalleeTurnOutcome`/`RunCallCapability`/`ParkQuestionInput`) at the root, then `utils/` (ported graph validation + Kahn topo order incl. `onDemandNodeIds` — the one call-only-node predicate shared by validation and execution; comment-preserving `workflow-yaml`; `turn-semaphore` — the callee sub-turn slot pool; `close-quietly`; `callee-text` — `calleeSummary`, the ONE line a caller is told about a callee: its label plus that node's own `description`, NEVER its `role`, shared by the `call_agent` tool description and the "May call" awareness block so neither can start leaking private instruction text), `services/` (`workflow-store` — the `*.geniro.yaml` library under `<userData>/workflows/`; `graph-executor` — the DAG fan-out over the M2 adapters, plus on-demand call-only callees, the per-run call surface, and per-caller-node MCP tokens. **A node's own settings are resolved ONCE at run start, and an unusable one is dropped with a system item rather than passed on** (`withResolvedNodeSettings` → `DroppedNodeSetting`): a `configDir` on a CLI with no such mechanism, an `effort` level the CLI's own `listEfforts` does not list. ONE type and one sentence template for both, because they are the same event — a value the builder would have refused, arriving on a workflow that came in as YAML, where the field was never offered. Dropping in silence is the failure this exists to prevent, and the cost of passing it through differs per CLI: claude warns and runs on its default, cursor answers `-32602` and the turn is lost. `effort` is a free STRING on the node like `model`, never an enum — the levels belong to the CLI, and one that gains a level must not need an app release; `call-broker` — call ids, depth/turn caps, sync/async+`await_agent`/fire-and-forget, the status envelope, and the M4 parked-question lifecycle (park → `answer_agent` / 5-min TTL → `QUESTION_TIMEOUT` / caller-settle drain → `QUESTION_ORPHANED`; a parked sync call becomes await-collectable); `mcp-server` — the stateless per-request MCP JSON-RPC host via `@modelcontextprotocol/sdk`, tools list/dispatch (`call_agent`/`await_agent`/`answer_agent`) + in-protocol error mapping; `capabilities` — composes `GET /v1/capabilities` from the adapter-owned probes AND each adapter's own `configDir.unavailableReason` and `handoffTarget` verdict, iterating the adapter registry rather than naming CLIs, and owning only the wire shape), `controllers/` (`workflows` + `mcp` + `capabilities` — thin routes delegating to their services), `dto/`. The **call runtime** replaced M1's run-start `GRAPH_CALL_RUNTIME_UNAVAILABLE` guard: a claude caller node gets `call_agent`/`await_agent` over a loopback MCP endpoint delivered as a per-turn `--mcp-config` file (the token rides the 0600 file, never argv), and a cursor caller gets the same endpoint in its ACP `session/new` frame (an HTTP header inside a stdin frame — never argv, never a file in the user's worktree). Every adapter hands its own CLI the endpoint, so **having outgoing call edges is the whole admission predicate** — there is no per-machine capability gate that can shut a caller out, and the M3 cursor trust probe, its `.cursor/mcp.json` merge lifecycle and the `cursorCalls` capability field were all deleted with the legacy transport. **The Q&A bridge (M4)**: a call-initiated callee's AskUserQuestion parks in the broker and returns to the CALLER as a `{status:'question', call_id, question, options}` envelope — the caller answers via `answer_agent(call_id, answer)` (delivered as `control_response` with `updatedInput.response`) or escalates to the user through its own question card, then collects the final result with `await_agent`. Because headless claude strips AskUserQuestion under `--dangerously-skip-permissions` (probe-verified on 2.1.202; re-probed on 2.1.227 against `system/init`'s own tool list, which sharpens the reason — the tool is wired by the presence of a permission-prompt CHANNEL, present under every mode with `--permission-prompt-tool stdio` and absent under every mode without it, so the bypass strips it by leaving the turn nobody to ask), question-capable claude turns — call-initiated callees AND caller nodes — spawn in the CLI's ask mode with the DAEMON auto-approving plain permission requests at the executor seam (unattended semantics preserved; an explicit `approval: 'ask'` node keeps the human card for permissions); the `requires_user_interaction` flag + tool name discriminate questions from permissions, and DAG-scheduled questions keep today's card path (now genuinely answerable: the WS `verdict` payload carries an optional `answer` that rides `updatedInput.answers` for a lone question and `updatedInput.response` for several — `withResponse` owns that choice, so no caller picks a field). Transcript kinds `call_question`/`call_answer` record the exchange under the caller's node. A cursor callee CAN raise one, over the vendor extension `cursor/ask_question` — baseline ACP has permission requests and no open-ended question call, so cursor added its own, and geniro used to decline it in-protocol with `-32601`. That did NOT stall the turn, which is what made it hard to notice: cursor's handler catches the refusal and replays each single-select question as a bare `session/request_permission`, options becoming `allow_once` entries. Measured against the pre-fix build, geniro rendered that as a generic permission card titled with the ask's `title` and no arguments, whose Approve picks the FIRST option — and under `auto` the daemon auto-approved it, answering the user's question with option #1 and no human involved. The lifecycle (park the JSON-RPC id → surface an `approval_request` → answer with the adapter's encoder) lives in the agent-agnostic driver; the method name, the params shape and the reply shape are four values the adapter injects as an `AcpQuestionProtocol`, so a second agent's channel is a config, not a branch. The contract is transcribed from the CLI's own shipped source (`8869.index.js` → `ask-question-handler.ts`), which is why `allowMultiple` is answerable here and not through cursor's fallback, which drops those questions entirely. It has still never been seen on the wire, so two guards remain: a payload that does not read as a question falls through to the old decline, and a free-text answer naming no offered option is sent as `skipped` with the text as its `reason` rather than as an invented `selectedOptionIds`. Whether a CLI's question channel COSTS the ask posture is its own adapter fact (`questionsCostAskPosture`) — true for claude, false for cursor, since an ACP request arrives whatever session mode the agent is in, so an unattended cursor node stays on `auto` instead of parking every permission on a verdict nobody is there to give.
- `v1/notifications/` — `notifications.module.ts` + `gateways/notifications.gateway.ts` (the Socket.IO channel: run rooms + the approval `verdict` round-trip; the verdict optionally carries an `answer` string for question cards — M4; plus the `debug` fan-out to the `debug` room, joined by `debug_subscribe` — a room and not a broadcast because with `agent-stdio` on it is thousands of lines a turn and only an open panel should receive them).
- `v1/diagnostics/` — **the debug log**, and the answer to "nothing anywhere records what this app did". The daemon's pino wrote to stdout, the supervisor piped that into the Electron shell's stdout, and a packaged shell launched from Finder has none — so every line was discarded in exactly the build a user would need it. `diagnostics.types.ts` (zod `DebugEntry`/`DebugChannel`/`DebugLogPage`/`DiagnosticsReport`), then `utils/` (`debug-sink` — a MODULE-SCOPE singleton, like `configureChildJournal` and for the same reason: it must predate Nest so the boot lines land, holding a bounded ring, an RxJS subject and a 0600 rotating JSONL file under `<userData>/logs/`; `redact` — registered-value scrubbing, at the ONE door every entry passes through, so a source added later is protected without its author knowing it must be; `pino-sink-stream` — tees the vendored logger via the new `ILoggerParams.streams`; `sink-logger` — a `ConsoleLogger` subclass installed with `Logger.overrideLogger`, which is what captures the OTHER logging path: `new Logger(X)` from `@nestjs/common`, used by nearly every service here plus Nest's own `InstanceLoader`/`RouterExplorer`/`ExceptionHandler`. Measured: 5 entries per boot without it, 80 with; `teeing-spawn` — a `SpawnFn` wrapper carrying the `agent-stdio` channel, composed at the one seam every adapter already shares so no CLI is named and the adapter contract is untouched), `services/` (`debug-log` — the injectable face plus the `AgentEventBus` subscription that IS the transcript channel; `diagnostics-report`), `controllers/`, `dto/`. Four channels: `daemon`, `transcript`, `ui`, and `agent-stdio` — the last **off by default**, because it is the user's own source code at megabytes a turn, so switching it on is their decision.
- `v1/handoff/` — **handing a conversation to the user**, which replaced the live PTY mirror and its whole plane (`TerminalSessionsService`, the `/terminals` Socket.IO namespace, `TurnMirrorService`, xterm.js in the renderer, and the `node-pty` dependency — one of the two native modules is gone). The mirror was a SECOND process rendering the same transcript, and it could not follow a running turn: probe-measured, an open `claude --resume` TUI grows by 0 bytes while another process writes the session, and **two interactive CLIs on one session do not see each other either** (A was told a codeword by B and answered "NOTHING-HERE"). Keeping it in step therefore meant re-spawning the CLI on a cadence — 0.8–3.0s of cold render per re-read, and a full repaint, because the new render is not an extension of the old (46% common prefix; the conversation grows ABOVE the fixed input block). What the mirror was reached for was never "watch a copy" but "let me carry this on myself", and that needs no syncing at all: the CLI is started when the button is pressed and renders the conversation as it stands then. So the daemon RESOLVES and never RUNS — `HandoffService` maps a run (or one workflow node, or one call thread) to `AgentAdapter.handoffTarget`, and the Electron main process opens the user's terminal with it (`main/open-terminal.ts` writes a 0700 `.command` script and `open`s it, so LaunchServices routes it to whatever the user set as their terminal — there is no macOS API for a default terminal, only that file association). `utils/shell-line.ts` renders the SAME invocation as one pasteable line, which the button shows on hover with a copy control, so a terminal geniro cannot launch (a remote host, an open tmux pane) is never a dead end. Layout: `handoff.module.ts` + `handoff.types.ts` at the root, then `services/`, `controllers/`, `dto/`, `utils/`.

**The cursor transport (ACP)** — every cursor turn runs under its **own throwaway `CURSOR_CONFIG_DIR`**, seeded with a copy of the user's `cli-config.json` and nothing else (`adapters/cursor-acp/utils/cursor-profile.utils.ts`, materialized by `prepareTurn` and swept at boot). Not hygiene — a correctness fix: applying a model or an effort over ACP PERSISTS into that directory, so before this a chat's model choice silently changed what the user's own `cursor-agent` opens with (measured: one `session/set_config_option` rewrote `model`, `selectedModel` and `modelSelectionHistory` in the real `~/.cursor/cli-config.json`). The copy keeps their permissions, approval mode and default model; `mcp.json` is deliberately NOT copied or symlinked — it is 0600 and holds their own server credentials, and it is unnecessary, which had to be measured rather than hoped: under a directory holding only `cli-config.json` an ACP session still loaded the folder's MCP servers and `mcp list` still reported all of them, because the CLI resolves that file from the user's home either way. Cost measured too: a handshake under a fresh directory took 5.9s against 6.5–7.7s on the default profile, so the ~690KB of CLI cache it writes per turn buys no latency penalty. The model listing takes its own profile for the same reason (a parameterized handshake can migrate a persisted variant selection, which writes). **The one thing that must NOT be per turn is the CONVERSATION**, and missing that shipped a build where every cursor chat's second message died: the CLI keeps each ACP thread at `<configDir>/acp-sessions/<sessionId>/` — inside the directory the disposer removes — so `session/load` answered `-32602 Invalid params {"message":"Session … not found"}` and the turn failed before its prompt (probed both ways on 2026.08.11-e8db854: the same load under the profile that created the session succeeds). So that ONE name is a symlink to `<userData>/cursor-sessions`, shared by every turn, while the settings the isolation exists for stay per turn — measured through as well: the CLI writes the store THROUGH the link, a second throwaway profile resumes the session and answers a codeword only turn 1 was told, and `node:fs` `rm -r` unlinks the symlink rather than descending into it. The store is deliberately NOT nested inside `cursor-profiles`, which the boot sweep removes wholesale. A `session/load` the agent REFUSES is now a degrade rather than a dead end — a `notice` naming the loss and a fresh `session/new`, because every later turn resumes the same id, so a hard failure made the chat unusable for good (`'session_load'` is a pending kind of its own in `acp-driver.ts` for exactly this).

**That session store is also where this CLI's context accounting lives** (`utils/cursor-context-store.utils.ts`), and reading it is the whole reason a cursor chat can show a breakdown at all. Over ACP the agent says NOTHING about its window — re-measured 2026-08-15 against a raw frame capture of a real tool-using turn, five `sessionUpdate` variants and a bare `{"stopReason":"end_turn"}`, with all three "context"-shaped keys being `embeddedContext` — so the adapter recorded "no usage" and the panel said so, which is what got reported ("Курсор должен как-то нам давать какие-то значения"). The figures were never absent, only off-protocol: the CLI writes a full breakdown per turn into `<sessionId>/store.db`, which is what its own TUI renders, and geniro already owns that directory because the per-turn profile symlinks into it. So this needs no new access, only a parser: `meta` holds one hex-JSON header carrying `latestRootBlobId`, `blobs` is content-addressed, and the root blob is protobuf — `{1: used, 2: window, 3: repeated {1: key, 2: label, 3: tokens, 4: chars}}`. Three things keep it honest. It is opened READ-ONLY through node's OWN `node:sqlite` (this file has nothing to do with geniro's ORM, and the built-in costs neither a dependency nor a rebuild against Electron's ABI) — geniro owns the directory but not the format, and nothing may write to another program's store. The block is found BY SHAPE rather than by field number, walking the message tree for rows of (string, string, varint), so a vendor adding a field still parses. And the arithmetic is re-checked rather than trusted: the categories must sum EXACTLY to the stated total, which is what the probe found to hold and what rejects a different block whose rows merely look alike — a genuinely changed encoding then yields null and no breakdown, never a number read out of unrelated bytes. An ESTIMATE was the obvious alternative and was measured to be unusable: 346 characters of transcript against claude's 23,575 `Messages` tokens on a comparable thread, ~250× wrong, because the window is dominated by tool and skill listings a transcript cannot see. Same expiry warning as every probe block in this adapter — the header also carries an unused `blobEncryptionKey`, which is the most likely way this stops working, and it stops by returning null.

`CursorAcpAdapter` drives `cursor-agent acp`, Cursor's first-party Agent Client Protocol server, over JSON-RPC on stdio via the agent-agnostic `adapters/acp/` client (`acp.types.ts` wire shapes, `acp-jsonrpc.ts` framing, `acp-driver.ts` the per-turn state machine). One turn is one process — spawn → `initialize` → `session/new`|`session/load` → optional `session/set_mode` → optional `session/set_config_option`|`session/set_model` → `session/prompt` → stop reason → exit — and `CursorAcpAdapter.canHostSession` DECLARES that, with the reason beside it: ACP has the shape for a kept process (`session/prompt` can be sent again on a loaded session), but no signed-in multi-turn ACP session has been observed here, and a second prompt on a session the agent considers finished is answered with the previous turn's context — a wrong turn, not merely a slower one. Claude, by contrast, now keeps its process BETWEEN turns (`AgentSessionRegistry`), so the two CLIs genuinely differ here. What is unchanged either way is the machinery around the process: `ProcessRegistry`, cancel and the executor's fan-out work the same for both. The "→ exit" at the end of that line had to be MADE true: a one-turn turn settles when its process ENDS, and closing stdin only asks — `cursor-agent acp` ignores EOF and lived on (measured: alive at 163MB with a `worker-server` grandchild 3m25s after reporting `end_turn`, and four such groups found reparented to launchd 1.5 days old). Two symptoms, one cause: the run's `ProcessRegistry` slot was never released, so every later message in that chat answered `RUN_BUSY` and sat in the composer's queue with the spinner still running, and each turn leaked a process group. So the terminal event now arms a short exit deadline (`TURN_END_EXIT_GRACE_MS` in `utils/spawn-cli.ts`) after which the group is terminated — a CLI that does exit on EOF is gone long before it fires, and the turn still settles on the process actually ending, so `done` still implies drained stdout. Permission requests are real (`session/request_permission` is an ACP baseline): `auto` auto-approves in the driver to keep unattended semantics, `acceptEdits` auto-approves `edit`-kind tool calls only, `ask`/`plan` park on a user verdict. A caller node's MCP endpoint travels in `session/new`, so the call token rides an HTTP header inside a stdin frame — never argv, never a file on disk. `session/load` resume suppresses the agent's transcript replay so a resumed turn cannot duplicate history. Pasted images ride as `image` content blocks in `session/prompt`, gated on the agent's own `promptCapabilities.image`. **Per-session model selection is real** and was mis-recorded as absent until probed, and it has TWO carriers because ACP replaced one with the other: `configOptions[]` (the entry of category `model`, ACP 1.0 / schema v1.16.0, applied with `session/set_config_option {sessionId, configId, value}` — `configId`, not the `configOptionId` the docs' prose uses, which the agent rejects) and the pre-1.0 `models.currentModelId` + `models.availableModels`, applied with the now-removed `session/set_model`. Removal from the SPEC is not removal from the BINARIES: cursor-agent 2026.08.04-aaa8809 sends both blocks and answers both methods, so `acp-models.ts` reads whichever the reply carries (configOptions wins) and the driver picks the frame from what the agent ITSELF advertised — never from a version number. `listModels` runs a two-frame handshake and the driver applies the model BEFORE the prompt. The ids are the handshake's own namespace: `cursor-agent models` prints a DIFFERENT spelling for the same model that the agent answers `-32602 Invalid model value`, so the subcommand can never be the picker's source. A resumed session is asked unconditionally, and the reason recorded here — "a `session/load` reply is not observed to list models" — was REFUTED once resume could actually succeed: measured on 2026.08.11-e8db854, a load reply carries `modes`, `models.currentModelId` AND a full `configOptions` list with each option's `currentValue`, so a resumed turn reads the same fields as a fresh one (`model=claude-opus-5` then `effort=xhigh` both ACCEPTED on a loaded session). It went mis-recorded because the reply could not be observed at all while the session store was being deleted with each turn's config directory. What stands is the CONCLUSION: silence is not a refusal, so nothing is refused locally on a reply that enumerated nothing. **Background sub-agents are real on this transport too**, and the adapter declared for two milestones that they were not — on a measurement that read geniro's own `acp.types.ts` and concluded something about cursor. Refuted on the wire 2026-08-13 (2026.08.11-e8db854): a delegation arrives as an ordinary `tool_call` whose `rawInput` is `{_toolName:"task"}` (its title is the placeholder `Task: Subagent task`, the description not yet known), and a `cursor/task` REQUEST then carries the description, the full brief, the type, the model and the duration — the method the adapter had listed as "the single most suggestive artifact found" and was declining on every delegating turn. The lifecycle is an agent-agnostic `AcpDelegateProtocol` (method + launch marker + params reader + the two facts below), so a second CLI's delegate channel is a config rather than a branch; the driver emits `subagent_info` twice per delegate — an anchor at launch, so the block opens and the run reads busy while it works, then the facts — and the renderer merges them last-non-null. Two adapter facts carry what the probe also found is NOT there: `subagents.stepsUnavailableReason` (nothing the delegate itself did crosses the wire — it runs as its own conversation in the CLI's private per-project store, so the block opens onto the brief and the duration and SAYS why, instead of "this sub-agent has not done anything yet" about one that worked for thirteen seconds) and `resultIsBookkeeping` (the `task` call returns `{durationMs, isBackground}`, which the block framed as `Result from <delegate>` — dropped, with the duration kept on the announcement). Known ACP gaps, surfaced as `notice` events (→ `system` items) rather than dropped: an unavailable session mode, a withheld call surface, images an agent never said it could read, and the FIRST unimplemented agent→client request per turn — Cursor's other blocking `cursor/*` extension requests (`create_plan`, `generate_image`) are declined in-protocol with `-32601`, and the notice flag is per-turn, not per-method; `cursor/ask_question` and `cursor/task` are the two that are answered instead. The legacy one-shot `-p --output-format stream-json` adapter, its `.cursor/mcp.json` merge machinery, and the MCP-trust probe were deleted with this transport.

- Binds **`127.0.0.1` only**. Assembled exactly like Geniro's `apps/api` (`buildBootstrapper(…)` → `addExtension(buildHttpServerExtension(…))` → `init()`); the loopback specifics ride on the extension's `host` / `portFallback` / `onListening` options — bind 127.0.0.1, negotiate a free port if the preferred one is taken, and write the pidfile from `onListening` after a healthy listen (the `http-server`'s own listen still defaults to `0.0.0.0`, preserving Geniro's behavior). Shutdown is Nest-owned (`enableShutdownHooks`); `utils/pidfile.lifecycle.ts` clears the pidfile on the way out, so `main.ts` needs no signal handling.
- Writes the pidfile (`daemon.json`: `pid`, `host`, `port`, per-launch `token`, `version`, `entry` and `startedAt`) **only after** the schema is synced and the server is listening, then prints `GENIRO_DAEMON_READY {port}` to stdout. The UI never assumes the host/port — it reads the bound values back from the pidfile (so it no longer passes `GENIRO_PORT`; the daemon owns that default).
- Config env (set by the UI): `GENIRO_USER_DATA` (userData dir; fallback `~/.geniro`), `GENIRO_PORT` (preferred port; default `47615` per `DAEMON_PREFERRED_PORT` in `utils/handshake.ts`, falling back to a free port if taken) and `GENIRO_IDLE_EXIT_MS` (self-shutdown window; absent = never, which is the default for every launch the supervisor did not spawn). DB is `geniro.db`, pidfile `daemon.json`, instance lock `daemon.lock`, child journal `children.json`, all in the userData dir.

### Daemon endpoints (loopback)
| Route | Purpose | Auth |
|---|---|---|
| `GET /health/check` | readiness probe (`{status, version}`) | public |
| `GET /metrics` | Prometheus metrics | bearer token (`LoopbackTokenGuard`) |
| `GET /swagger-api` · `/swagger-api-json` · `/swagger-api/reference` | OpenAPI spec (the source for `pnpm generate:api`) + Scalar UI | bearer token (`LoopbackTokenGuard`) |
| `/ws` (Socket.IO) | renderer ⇄ daemon channel: `join`/`leave` run rooms, streamed `item` events, approval `verdict` → `verdict_ack`, plus `run_status`, a client-wide broadcast that carries no room so every chat's badge stays live, not just the focused run's. Its `status` is **nullable**, and that is what separates a status change from an activity announce: the announce fires from inside a turn's event stream on every tool call and never reads the run, so the hardcoded `'running'` it used to send was a guess the client wrote onto the row — one straggler arriving after a cancel flipped the badge back to running, with nothing announcing again to correct it. A null status says only "this is what the run is DOING" and leaves the badge alone. Its `activity` has two consumers, not one: the sidebar badge, and the transcript's own live row through `RunActivityContext` (`chats/live-row.tsx`), so the two can never name different work for the same run | per-launch token (handshake `auth`) |
| `POST/GET /v1/chats*` | single-agent chats: create/list, `:runId/items` history (`afterSeq` cursor, shared by workflow runs), `:runId/messages` (text + pasted `images`), `:runId/attachments/:attachmentId` (one attachment's base64 bytes — an `<img src>` can't carry the bearer header), `:runId/cancel`. Create also takes an OPTIONAL `configDir` — the agent CONFIG directory this chat runs under (the folder holding that CLI's credentials, settings, plugins and history), so a chat can run on a **different account — a different subscription** — with a different toolbelt, without touching the user's default profile. It travels as ENV, never argv (`CLAUDE_CONFIG_DIR`; claude exposes no flag), which is why the adapter maps it in `buildEnv`. Part of the run's IDENTITY like `cwd`: canonicalized by `resolveValidConfigDir` at creation, re-resolved on every turn, stored on the run row, and NOT carried by the settings PATCH. Existence is checked because the CLI will not check it FOR the user — probe-verified on 2.1.227, claude CREATES whatever directory it is handed and ends the turn "Not logged in", so a typo silently starts a brand-new signed-out profile instead of failing. A CLI with no such mechanism REFUSES it (`CONFIG_DIR_UNSUPPORTED`, the adapter's own sentence) rather than stripping it the way the graph executor must for an imported workflow: a chat's profile is picked interactively, so the honest answer is "no". A `POST /messages` **while a turn is running** is handed to that turn rather than refused, when the CLI has a channel for one — claude's stream-json stdin accepts a second user message and acts on it at the next tool boundary (probe-verified on 2.1.222), so the CLI gets it in seconds instead of after the process exits. Delivery goes through `AgentTurnHandle.sendUserMessage` (→ `AgentAdapter.buildFollowUpPayload`), and the CLI is told FIRST: only a delivery it confirmed is written to the transcript, since the reverse leaves a user message on screen that no agent received. A CLI with no such channel (ACP's `session/prompt` is one request per turn) answers false and the route 409s `RUN_BUSY` exactly as before, which is what the renderer queues on | bearer token (`LoopbackTokenGuard`) |
| `DELETE /v1/chats/:runId` | **destructive and irreversible**: cancels a running turn, then hard-deletes the run's `runs`/`items`/`node_state` rows with the `softDelete` filter disabled, removes `<userData>/attachments/<runId>/`, kills its live PTY mirrors and revokes its call tokens (the teardown itself is `RunTeardownService`, shared with the workflow-run delete). Chat runs only — a workflow run is rejected, since deleting one through this route would skip the graph executor's teardown; it goes to `DELETE /v1/workflows/runs/:runId` instead | bearer token (`LoopbackTokenGuard`) |
| `GET /v1/chats/:runId/image?path=` | an image an agent referenced from its OWN markdown (`![shot](/tmp/shots/x.png)`), read off disk as base64 (`LocalImageService`). Two independent walls made every such image a broken box: the renderer's CSP is `img-src 'self' data:`, so a `file:` source is refused outright, and a RELATIVE reference resolves against the app's origin rather than the folder the agent was working in — so a relative path is resolved here against the RUN's cwd, which is the only base under which `![](a.png)` means anything. Both forms measured in the author's own history (`/tmp/shots/…` and bare `a.png`). Deliberately NOT confined to that cwd: the real references sit outside any project folder, so a containment rule would refuse the real case while protecting nothing — the reference was written BY the agent, which already reads the disk with the user's privileges, and the bytes go to a renderer whose CSP forbids sending them anywhere. What IS enforced keeps it an image channel rather than a file-read one: a known image extension checked BEFORE the file is opened, a real regular file, a size ceiling, and canonicalization before the stat so the checks and the read decide about the same file. A URL is refused rather than resolved — `resolve()` would turn `https://evil/x.png` into a directory under the cwd | bearer token (`LoopbackTokenGuard`) |
| `GET /v1/chats/:runId/metrics` | what this chat's window currently HOLDS, and what the thread has cost (`ChatMetricsService`). Two halves with two sources, answered together so the panel can never show a breakdown from one moment beside a spend from another. The breakdown comes from `AgentAdapter.readContextUsage` and **each CLI answers on its own channel**: claude is ASKED over the live process's stdin control channel (`CliSession.ask`), so its reading exists only while the run holds a session — measured at 1.2–3.3s, which is why the whole route is fetched when the readout is OPENED and never polled; cursor holds no process between turns and is READ off its own session store instead. The totals are summed from the run's own `turn_complete` rows on the DAEMON, because a client that has paged only part of a long transcript would silently total only part of it. Absent figures are an answer, not an error: a CLI with no such channel and a claude chat whose process has been reaped both come back 200 with `breakdownReason` saying which, and only the first will never change | bearer token (`LoopbackTokenGuard`) |
| `GET /v1/agents/models` | the models one agent CLI accepts for `--model`, asked of the CLI itself (`ModelsService` → `AgentAdapter.listModels`), cached per `--version` + TTL | bearer token (`LoopbackTokenGuard`) |
| `GET /v1/agents/efforts` | the reasoning-effort levels one agent CLI accepts (`EffortsService` → `AgentAdapter.listEfforts`). **Both** CLIs list levels: claude's `--effort` vocabulary, and cursor's `low/medium/high/xhigh/max`. Cursor's was `[]` for months on the recorded grounds that it folds effort into an opaque model id and rejects every recomposed one with `-32602` — all true, and the conclusion still wrong: it is a property of the HANDSHAKE, not the CLI. `clientCapabilities._meta.parameterizedModelPicker` (read out of the CLI's own bundle, `2996.index.js` → `clientSupportsParameterizedModelPicker`) switches `session/new` from one composed id per model family to a BARE name plus one config option per parameter — `effort`, `thinking`, `context`, `fast`, each with its own enumerated values. geniro never sent it. Measured on 2026.08.11-e8db854: `model=claude-opus-5` then `effort=xhigh` are both ACCEPTED (the same value recorded as rejected before), `effort=max` accepted, `effort=bogus` `-32602`. ORDER is load-bearing — the options are derived from the CURRENT model and a fresh profile opens on `auto-smart`, which has none, so `effort` before `model` is `Unknown model config option`. The adapter splits a stored id into `{model, parameters}` (`utils/cursor-model.utils.ts`), the turn's own effort winning over one baked into a legacy id, and the agent-agnostic driver applies model-then-parameters. A CLI that lists NO levels still gets no picker, but not silence: `EffortSelect` renders an inert chip carrying `AdapterConfig.effortsUnavailableReason` (on the wire as `modelEfforts[]`), because a control that simply vanishes is what got "I cannot change the effort" reported. Full probe + re-check triggers at `CursorAcpAdapter`'s `efforts` field | bearer token (`LoopbackTokenGuard`) |
| `GET /v1/agents/mcp` | the MCP servers one agent loads in a given cwd, with the live health the CLI reported (`AgentMcpService` → `AgentAdapter.listMcpServers`); `refresh=true` re-dials instead of serving the cached reading. A COLD read does not block: dialling every server the folder defines is measured in seconds, so past a short budget the route answers `pending: true` with empty rows and finishes the dial behind the response — the renderer's hooks re-ask until it settles. So only `servers: []` with a NULL `unavailableReason` **and `pending: false`** asserts that the folder has none — a non-null reason covers both a CLI that cannot be listed at all and a read that failed or could not be parsed, so "no servers here" and "we are not saying" stay distinguishable without any caller branching on agent kind. Each row also carries `scope` / `disabled` / `toggleUnavailableReason`, composed from `AgentAdapter.readMcpFolderFacts` on EVERY exit path (cache hit included), because the LISTING cannot see the toggle — `claude mcp list` reports a disabled server as though it were live | bearer token (`LoopbackTokenGuard`) |
| `PUT /v1/agents/mcp` | switch one server on or off for one `(agent, cwd)` and answer with the recomposed listing. The write goes through `AgentAdapter.setMcpServerEnabled` into the CLI's OWN state — for claude, `projects[<cwd>].disabledMcpServers` in `~/.claude.json`, taken under the same `proper-lockfile` lock the CLI takes; for cursor, that CLI's own `mcp enable|disable` subcommand run IN the folder — so it covers servers of EVERY scope and the switch is the same one the user's `/mcp` panel shows. **Both** shipped CLIs switch now: cursor's refusal stood for two milestones on the recorded reading that `mcp enable|disable` write a global `cli-config.json`, which is why every cursor row rendered a padlock while the user's own Cursor UI offered live switches for the same servers. Refuted on 2026.08.11-e8db854 — the CLI's own bundle resolves `disabledPath` as `<projectDir>/mcp-disabled.json` off `gitRoot(cwd) ?? cwd`, `cli-config.json`'s checksum is unchanged across a write, and a `disable` in one directory leaves another's listing untouched. So the grain is claude's grain, and the old measurement was per-REPOSITORY seen through two folders of one repo. geniro drives the subcommand rather than writing the file, because the path also needs `CURSOR_DATA_DIR`/`XDG_CONFIG_HOME` and the right one of the CLI's two project-dir functions (the other sha256s the path past 92 chars). One consequence is in the SERVICE, not the adapter: a CLI whose disabled state is visible only in its listing has nothing for `readMcpFolderFacts` to report, so `AgentMcpService.patchCachedStatus` re-states the switched row in every cached reading of that folder — without it a cache hit put the switch straight back for the rest of the TTL. OFF becomes `disabled` (measured). ON is ASKED rather than assumed, through `AgentAdapter.readMcpServerHealth` — a ONE-server dial both shipped CLIs have a command for (`cursor-agent mcp list-tools <name>`, `claude mcp get <name>`, the latter a health check by its own `--help`) at 1.2–3.7s against the folder's 4–9s, so a re-enabled row shows its real health (a green dot, or `needs_auth` WITH its Sign-in control) instead of a dead `unknown`. Only a CLI with no such command, or an answer the parser could not read, still falls back to `unknown`. cursor's probe needed a new base primitive: `AgentCommandOptions.captureDiagnosis` resolves with stdout AND stderr whatever the exit status, because the two readings worth telling apart there — needs signing in, versus broken — both exit 1 and both write to stderr, so the usual null-on-failure collapsed them into each other. Still REFUSES rather than silently no-opping: a server the user turned down in their own settings, which claude's unioned rejection list makes un-re-enableable (cursor declares no such lock — `mcp enable` undoes the only state its switch writes) | bearer token (`LoopbackTokenGuard`) |
| `GET/POST /v1/groups` · `PATCH/DELETE /v1/groups/:groupId` · `POST /v1/groups/reorder` | the chat sidebar's **groups** — the folders a user files their threads into (name, colour, folded state, order, and an optional auto-file folder). `POST /reorder` takes the whole ORDER as a list of ids rather than "move this one up": the sidebar reorders by DRAGGING, where the gesture produces an arrangement, not a displacement — which also makes the write idempotent, where a relative move replayed moves twice. It is declared ahead of the `:groupId` routes so `reorder` is never read as a group id. A group the client did not name keeps its place at the end and an id naming nothing is ignored, so a stale list (a group made in another window, a delete that landed mid-drag) costs no one their position. **`DELETE` removes the GROUP and never its chats**: the runs filed under it are released back to the loose list with every item they hold, which is why the ack counts `released` rather than `deleted` | bearer token (`LoopbackTokenGuard`) |
| `PUT /v1/chats/:runId/group` | file one run into a group, or (null) out of every group. On the RUN's route because the run is what changes and what the answer is — the sidebar replaces the row it already holds. Kind-blind like `PATCH :runId` (rename): the sidebar lists chat and workflow runs together and a group holds either, so this is a run-row property rather than an execution command that has to reach the right engine. The group is CHECKED before the write — a dangling id renders exactly like a run that never moved, so the refusal has to happen here or not at all | bearer token (`LoopbackTokenGuard`) |
| `GET /v1/agents/skills` | skills + slash commands one agent kind accepts in a given cwd (the adapter's on-disk scan, project + `~`, merged with that CLI's own harvested `slash_commands` report and its self-reported command list) — feeds the composer's `/` autocomplete | bearer token (`LoopbackTokenGuard`) |
| `/v1/workflows*` | workflow library CRUD (`GET/POST /`, `GET/PUT/DELETE /:slug`, `POST /import`, `POST /:slug/export`) + runs (`POST /:slug/runs`, `GET /runs`, `GET /runs/:runId/nodes`, `POST /runs/:runId/cancel`, `DELETE /runs/:runId` — **destructive**: one run's history, never the library workflow it ran) | bearer token (`LoopbackTokenGuard`) |
| `GET /v1/handoff/login` | how the user signs a CLI ITSELF back in — the account, not one of its MCP servers, which `GET /v1/handoff/mcp-login` covers and which fixes a different failure. Resolve-only, like every handoff route: the daemon answers with the invocation (`claude auth login` / `cursor-agent login`) and the Electron main process opens the terminal. A turn that failed on a lapsed session carries `recovery: 'cli-login'` on its error item, which is what puts a Sign-in control on the row | bearer token (`LoopbackTokenGuard`) |
| `GET /v1/handoff` | how the user reopens this conversation themselves: the CLI invocation for a run/node/thread, or the reason there is none. A refusal is a 200 carrying `unavailableReason` — "this CLI cannot reopen its conversations" is the ANSWER, and the button renders it disabled with the reason on hover; only a malformed request (unknown run, a node that is not an agent) is a 4xx. The invocation carries the chat's own model AND its own `configDir`, the latter as `env` on the wire (`HandoffTarget.env`, rendered into `display` as a `NAME=value cmd …` prefix and into the Electron `.command` script as `export` lines ahead of its `exec` — NOT the same way, and the reciprocal TWIN PARSER blocks on `shell-line.ts` / `open-terminal.ts` say why: a prefix assignment after `exec` makes the shell resolve `NAME=value` as the program to run) — because a config directory has no argv form and the session being resumed lives INSIDE that profile: dropping it does not merely open the wrong account, it opens an unrelated conversation, since the id it resumes is not in the default profile's store. The same env rides `GET /v1/handoff/login` and `/mcp-login`, so a sign-in fixes the profile the run actually uses. A WORKFLOW node's is null — `node_state` stamps no config directory, so run history cannot say which one a finished node ran under | bearer token (`LoopbackTokenGuard`) |
| `GET /v1/diagnostics/logs` · `PUT /v1/diagnostics/settings` · `POST /v1/diagnostics/ui-log` · `GET /v1/diagnostics/report` | the debug surface: read the log (`afterSeq` cursor, and a `dropped` count said OUT LOUD rather than left as a silent gap), choose which channels are recorded, hand the daemon a line the RENDERER produced (so a UI throw lands in the same ordered stream as what the daemon was doing at that instant, instead of in a console nobody can hand you), and take the one-paste report (versions, paths, each CLI as the daemon can actually see it, run counts, and the redacted log tail). Live entries ride the `/ws` `debug` room instead | bearer token (`LoopbackTokenGuard`) |
| `POST /v1/mcp/:runId/:nodeId` | the agent-to-agent call runtime: a stateless MCP JSON-RPC endpoint (`initialize` / `tools/list` / `tools/call` for `call_agent` + `await_agent` + `answer_agent`) scoped to one run's one caller node; GET and DELETE on the same path answer 405, a stateless server having no SSE stream and no session to tear down | that caller node's **per-node call token** OR the launch token (`LoopbackTokenGuard`) |
| `GET /v1/capabilities` | machine capabilities for the builder and the chat approval chip: the claude permission-mode probe verdict (`claudeModes`) and, per CLI, whether it can be pointed at a config directory (`configDirs[]`, carrying each adapter's own `configDir.unavailableReason` — cursor declares one, because it READS a config directory but keeps the account outside it: probed 2026-08-12, a fresh empty `CURSOR_CONFIG_DIR` receives a `cli-config.json` carrying no `authInfo` and `cursor-agent status` under it still reports the DEFAULT account, so pointing a run at one would not change the subscription) whether it can reopen its own conversations interactively (`interactiveTerminals[]`, read through each adapter's own `handoffTarget` — the renderer reads this rather than allowlisting an agent by name), and whether it offers a reasoning-effort picker (`modelEfforts[]`, carrying `AdapterConfig.effortsUnavailableReason` — `[]` from `GET /v1/agents/efforts` hides the picker, and this is what lets its absence explain itself; null for both shipped CLIs today); reading pre-warms the probe when it has not run. Each adapter reads only its OWN field, via `AgentAdapter.approvalSupportFrom` — cursor declares no probed mode, so it reads nothing here | bearer token (`LoopbackTokenGuard`) |

The public allowlist is **`/health` alone** (`PUBLIC_PREFIXES` in `auth/token.guard.ts`), matched at segment boundaries so a sibling route like `/health-debug` doesn't inherit it. `/metrics` and `/swagger-api` are **token-gated**: with a deterministic default port, any web page could otherwise read the daemon's Prometheus internals and full API schema cross-origin. Every non-allowlisted route requires the `Bearer <token>` header — the launch token, or, **only** on `/v1/mcp/<runId>/<nodeId>`, that caller node's own per-node call token (minted when the run starts, revoked when it settles, keyed by `(runId, nodeId)` so one callee child can't open another node's route). The WS channel is a NestJS Socket.IO gateway (`@WebSocketGateway({ path: '/ws' })`) installed via the `IoAdapter` in `main.ts`; the renderer (`socket.io-client`) sends the per-launch token in the Socket.IO handshake `auth` payload (browsers can't set headers on a WS handshake). The HTTP `LoopbackTokenGuard` doesn't see Socket.IO traffic (engine.io intercepts `/ws` before Nest routing), so each gateway owns its own WS auth — via the one shared `auth/ws-auth.ts` → `enforceWsHandshakeAuth` (constant-time `safeEqual`, disconnects on mismatch), called from `handleConnection` in the notifications gateway. It stays extracted precisely so a second gateway cannot reintroduce a divergent copy of the check.

### The UI (`apps/ui`)
electron-vite project. `src/main/` — `index.ts` (app lifecycle), `daemon-supervisor.ts` (spawn/adopt/health-poll/orphan-sweep), `daemon-pidfile.ts` (reads + validates the daemon's pidfile), `settings.ts` (`settings.json`), `purge-legacy-secret.ts` (deletes the one Keychain entry earlier versions wrote — see *Storage split*), `cli-detect.ts` (per-CLI binary + version, plus whether the CLI reports itself signed in, for a CLI that can be asked — `loggedIn` is three-state, and `null` means UNKNOWN rather than signed-out, because a failed probe must not tell a signed-in user to sign in; the answer is read from the CLI's own STRUCTURED reply (`cursor-agent status --format json` → `isAuthenticated`), because its human output has a third `partially-authenticated` state that matched neither wording and read as ready), `probe-env.ts` (the main-side twin of the daemon's `child-env.ts`: a probe child gets its OWN agent's credentials and never the other's — `detectClis` runs an authenticated `status` call, so it must not hold a rival agent's token), `git-info.ts` (the composer's branch chip: repo/branch/branches/dirty, plus a branch switch GUARDED by a re-read dirty check — and `git switch`, never `git checkout`, which would resolve a branch name as a pathspec and restore a file instead), `ipc.ts`, `updater.ts` (notify-only update check: polls GitHub Releases and points at `brew upgrade`, never self-updates; dev no-op, launch check gated by settings), `login-shell-path.ts` (packaged Finder launches inherit launchd's minimal PATH — resolve the login-shell PATH for the daemon), `notifications/` (**system notifications** — `notifier.ts`, the ONE file that touches a notification API, and `notifications.service.ts` over it). `src/shared/contracts.ts` holds the Electron-internal contracts only — IPC/Settings/CLI + `DaemonHandle` — shared across main/preload/renderer; it deliberately holds **no daemon wire types** (those are generated, see *The daemon API client* below). `src/preload/index.ts` exposes a typed `window.geniro` via `contextBridge`. `src/renderer/` — React app (`App.tsx`, `onboarding/`, `chats/` incl. `approval-card`, the composer's `/` skill autocomplete (`skill-menu`) and its chip row (`folder-select` and `config-dir-select` — both built on `directory-select`, the ONE directory chip (recents + the current value + a native-picker row), which is also what keeps them from drifting apart; the config one adds a "Default profile" row, since the CLI's own is its normal state, and renders NOTHING for a CLI whose `GET /v1/capabilities` `configDirs[]` row names a reason — never a disabled chip, the same rule the effort chip follows. In an OPEN thread it is stated once, in the header beside the agent (`chat-header`), because it is the same kind of fact — fixed for the run's life, and about what the conversation IS rather than where the next message goes; a run on the default profile shows no chip at all; `branch-select` — git branches, only for a repo; `model-select` — the models the CLI ITSELF reports (via `GET /v1/agents/models`) + a "default model" row, hidden for a workflow target; `approval-mode-select` — offered for **both** CLIs, each from its own `AdapterConfig.approval.modes` via `GET /v1/capabilities`, never from an allowlist of agent names. The ACP transport gave cursor real permissions (`session/request_permission`), so `auto`/`ask`/`acceptEdits` are a genuine choice there. What still differs is MID-TURN: only claude overrides `buildApprovalModePayload`, so changing the chip while a cursor turn is running is refused with a 409 `RUN_BUSY` — the renderer shows the daemon's own sentence for it (`daemonErrorDetail`), not raw JSON, and the change lands on the next turn), pasted-image attachments (`use-attachments` staging + `attachment-strip` thumbnails on the composer; `attachment-payload` + `message-attachments` in the transcript, fed by the `AttachmentLoaderContext` `Chats` provides so the memoized row shells need no new props), `settled-status.ts` — the ONE reading of "which run status does this terminal item imply", and the rule that goes with it: **only a LIVE terminal item mirrors its status onto the run row**, while a REPLAY takes one reading from its LAST item (`reconcileFromTail`). Every chat past its first turn replays a `turn_complete`, so mirroring each replayed row wrote a past turn's ending onto a run that was working — measured on the real app as a chat reading `running · Working… 3m 39s` and then, after switching to another chat and back, `completed` with no live row, while the daemon still said `running`. The wrong value outlived the visit, which is the second half: the next activation read the row as settled and left `streaming` false, taking the transcript's live row with it. The two live rows' clocks follow the same principle — both count from a timestamp derived from durable rows (`ChatHeader`'s `turnStartedAt`, the working row's `workingSince` from the last row its own thread put on screen, the thinking row's daemon-published `thinkingSince`), never from their own mount, or every remount restarts them). **A run settling is a MOMENT, not a flag** (`RunSettledContext` → `RunSettleAt`, read by `subagentBlockStatus`): a delegate that has produced rows SINCE the run row settled is still working, whatever that row says, so the settle only closes blocks whose last row precedes it. As a bare boolean it painted every unreturned delegate in the chat `stopped` at once — measured on the author's own `geniro.db`, run `f42bfe2d` took 1346 further delegate rows after the item that settled it, and that is the reported "I can see stopped status - but it's incorrect, in fact they continue working". Three states and not two, because a settle whose timestamp will not parse is not the same as no settle: `'unknown'` keeps the old conservative reading rather than silently putting every spinner back. The seq-based `SubagentBlockEntry.closed` is untouched and outranks it — a turn-end ITEM after the delegate's last row is direct evidence that nothing more is coming. A tool group asks only `!== null`, since a tool call is one round trip and "has it spoken since" is the question `pair.result` already answers), the agent's own task list (`task-payload.ts` — the twin parser plus the FOLD, since a patch names only what moved; `task-list.tsx` — the `TaskListCard` the transcript shows where the list moved, and the shared `TaskRows` the SIDE PANEL composes for the current list (`agents-panel.tsx`, above that agent's threads and deliberately not behind a disclosure). The panel and not a strip over the composer, because the list is the same kind of per-agent state as the delegate rows beside it — a sub-agent likewise has a block in the flow AND a row in the panel. A delegate's list stays in its own block, whose collapsed header carries its count, since the block is closed by default. **An `in_progress` row only SPINS while something is working through that list** (`taskCardIsLive`): a turn that ended mid-task left a spinner running for good — the reported "finished one task, still in progress for some reason" — and every historical card animated its own in-progress task besides, so a three-step job showed three spinners for one running task. Two conditions, both needed: only a thread's LAST card can be live, and only while the run has not settled past it — read as a MOMENT, like `subagentBlockStatus`, so a card written after the settle (the CLI carrying on by itself) still spins. **That moment is the run's own terminal ITEM, not merely its row's `updatedAt`** (`settled-status.ts` → `lastTerminalItemAt`, whose later-of-the-two both `runStoppedAt` and `activeRunSettledAt` now take): items never touch that column, so it is only as fresh as the last thing that DID write the row and can sit BEFORE the stop it describes. Measured on a cancelled cursor turn — the daemon recorded the cancel at 07:24:50.871 while the row said 07:24:48.881, and a task announcement written at 07:24:49.462 fell inside that gap, so it read as work produced AFTER the settle and spun on a run the user had just stopped. The same gap sat under every `subagentBlockStatus` reading. A stalled row keeps a glyph of its own rather than falling back to `pending`'s: the agent started that task and stopped there), **how long each turn WORKED** (`turn-duration.ts` — the twin parser over `usage.durationMs`/`apiMs`, the wall-clock fallback, `TurnDurationContext` and `formatDuration`; rendered on the `turn_complete` row beside the cost it already carried, and summed into the header as `worked 4m 12s / 6 turns`). The app showed how long AGO a thread last spoke and never how long it worked: the header clock and the working row are live-only and both vanish at the settle, while a sub-agent block has said `took 13s` since M3 — so a delegate was better instrumented than the agent that launched it. Two clocks, and the CLI's own wins where it exists: claude's `result` line carries `duration_ms`/`duration_api_ms` (probed 2026-08-14; every timing field was being dropped by `readClaudeUsage`, which read only `total_cost_usd` beside them) and measures the agent WORKING, so it counts neither the first-prompt MCP-readiness hold nor a stretch parked on an approval card. ACP reports no timing at all and declares so rather than passing off the driver's own wall clock as the agent's figure, so a cursor turn is measured from its durable rows — MINUS the parked stretches, which is what keeps the fallback meaning the same thing as the primary: an `approval_request` and the `approval_verdict`/`unanswerable` closing it are both rows, so the wait is readable, and without the subtraction a turn blocked half an hour on a question card would report the lunch break as work. The thread total is the SUM of its turns and never `updatedAt - createdAt`, which is the plausible-looking wrong number — a chat left open across three days with five minutes of work in it spans `3d` and worked `5m`, and both are now on the header at once. `apiMs` can EXCEED `durationMs` on a turn served off a kept session (measured: turn 1 `3007`/`2504`, turn 2 `2562`/`4633`), so the tooltip's API/own-work split is withheld on that reading rather than clamped to a fabricated `0s`), **what the context window actually HOLDS** (`context-panel.tsx` + `chat-metrics.ts`, opened by the existing `context-meter` ring). The ring answered "how much room is left" and nothing about what filled it, which is the whole of the report: a user pointing at `9%` and asking for the statistics their own CLI's `/context` prints. Fetched from `GET /v1/chats/:runId/metrics` when the readout is OPENED and never polled — it is a multi-second question put to the user's own running agent — and re-fetched on every open rather than cached, since the window is exactly what changes between two glances at it; the previous reading stays on screen while the new one lands, so the re-fetch costs no blank panel. One component still, not a second meter: the panel is `children` the same `MeterReadout` renders, and the one-line summary steps aside only once a full breakdown has arrived (it states the same used/window pair, and two renderings of one figure inches apart is what the readout was already criticised for). Deferred categories are drawn in their OWN section rather than in the bar, because they sit outside the CLI's total — one live reading had 274k deferred against a 98k total, which in one bar reports a window nearly four times fuller than it is. Category colours come from the avatar palette by POSITION in the CLI's own list, never by name: the names belong to the CLI and it is free to invent one, which a name-keyed map would leave uncoloured or, worse, silently give a neighbour's colour. Offered for a CHAT only — a workflow run's nodes each hold their own process, so `metricsRunId` is null there and the panel is never shown under a name it does not describe), `graphs/` React Flow canvas (**a node's pickers are only as trustworthy as the list behind them, and the list arrives late.** The inspector fetches the SELECTED node's CLI vocabulary, so a node of the other CLI mounts against the previous one's — and `model-select` acts on it twice, adopting the first alias for a model-less node and deciding whether a stored model is off-list. Measured 2026-08-15 in the real builder: adding a cursor node while a claude node was selected wrote claude's `claude-fable-5[1m]` onto the cursor node, into the workflow YAML and then into the run — whose own transcript recorded `agent does not offer the model 'claude-fable-5' — this turn runs on the agent's current model instead`, so the node ran on a model nothing on screen named. Both directions reproduced (a claude node stamped `auto-smart`), deterministically, with both lists already cached. The fix is in `useAgentVocabulary`, which now resolves DURING RENDER off its own cache rather than from an effect-set state: an effect is always one commit late, and that commit is the one a child mounts in. `model-select`'s second use is decided the same way — the mode waits for a non-empty list, which also ended the visible half, every re-opened node showing its model as `Custom…` over a text input. The **canvas** carries the same lateness in geometry: React Flow defers the initial `fitView` until nodes are measured, so on a workflow created moments ago it fired when the FIRST node landed and scaled that one node to fill the pane — `scale(1)` empty, `scale(3.42)` the instant a node was added — hence `INITIAL_FIT`'s `maxZoom: 1`, capping the AUTOMATIC fit only; the Controls' own fit button is asking to fill the screen and stays uncapped. A toolbar add also PANS to what it just added: adds stack 260px right of the rightmost node, so the fourth click put its node past the edge and the builder read as having stopped adding them — a drop is exempt, having landed where the user aimed). The inspector's **reasoning-effort** row reuses the composer's `EffortSelect` under its `default` (bordered) variant rather than a second picker in `graphs/` — the rows, the default sentinel and the add-back row for a level the CLI no longer lists are what is worth getting right once. Unlike Model there is no ADOPT: an unset effort means "let the CLI decide", which is a real answer, so picking the first level on the user's behalf would change how every new node thinks), `settings/`, `notifications/` (the renderer half of the notifications module — see below), `autogenerated/` the generated daemon client + every daemon wire type (`pnpm generate:api`), `daemon-api.ts` which binds it to a launch handle, `handoff-open.ts` the ONE resolve-then-open-a-terminal helper (four callers across two screens — a thread's conversation, an MCP server's sign-in, and the CLI's own sign-in from both the chat surface and Settings), `daemon-client.ts` the WS client).

**macOS packaging (M4)**: `pnpm --filter @geniro/ui build:mac` runs `scripts/build-mac.mjs` — two self-contained `pnpm deploy --prod --legacy` stagings (shell app + daemon under `Resources/daemon`), Electron-ABI rebuild of better-sqlite3 in the daemon staging (node-pty ships an N-API prebuild and deliberately stays OUT of `rebuild:native`), `.icns` generated from `resources/icon.png`, ad-hoc (unsigned) signing — no Developer ID, no notarization, no `publish` feed; the Homebrew cask + install script strip the quarantine bit so the ad-hoc app launches, and the app is notify-only (points at `brew upgrade`), DMG + zip into `release/dist/`. The packaged app presents as **"Geniro"** on every macOS surface (menu bar, Dock, About, Finder) via electron-builder `productName`.

**System notifications** — a macOS banner when an agent stops to ask something and when a thread's turn ends, so a run parked on a question does not sit unanswered while the user is in another app. **One module, split across the two processes by what each can answer**: `renderer/notifications/` decides WHEN (`run-notifications.ts` — pure rules over the DISPLAY statuses the sidebar badges show, never the daemon's row, so a banner and the row it sends you to cannot describe one run differently; `use-run-notifications.ts` — the thin React binding, mounted from `Chats`, which the shell keeps mounted across nav switches so a thread finishing while the user is in Settings still reports), and `main/notifications/` decides WHETHER and does the posting. Whether it becomes a banner is read from `notificationsEnabled` at the moment of the post rather than cached in the renderer, so a flipped switch takes effect on the very next question; keeping the gate in the one process that can post one makes that true by construction. Three rules the diff enforces, each a defect if dropped: a run **seen for the first time never notifies** (the chat list loads with every past thread already finished — otherwise the app opens with a banner per conversation in the history), a turn the user **cancelled** is not announced (telling somebody what they just did with Stop is the notification everyone switches the feature off over — `failed` IS announced, since nobody asked for it), and the chat the user is **already looking at** is skipped, but only while the window has focus (suppression is about watching, not about which chat is open — without both halves a user who leaves the app sitting on a running chat is told nothing at all). Clicking a banner raises the window in main — a renderer in a hidden window cannot show itself — and sends the run back over `onNotificationActivated`, which `App` reads for the view and `Chats` for the thread. `notifier.ts` is the only file that touches a notification API, and Electron's own `Notification` IS the ready-made library here: an npm package (`node-notifier`) posts through a bundled `terminal-notifier.app`, so the banner is attributed to that helper rather than to Geniro (and the per-app switch in System Settings then names the helper), it ships a nested Mach-O binary into an app that is ad-hoc signed with no Developer ID, and its click cannot reach into this process to raise the window. **Measured 2026-08-15**: `Notification.isSupported()` is true and the service posts/suppresses correctly, but no banner renders on a Mac while the display is being shared or recorded — `dndMirrored: true` in `com.apple.ncprefs` suppresses every app's, which an `osascript display notification` control probe confirms is not specific to this code.

**Sidebar groups** (`chats/run-group.ts`, `chats/group-header.tsx`, and the `run_groups` rows behind them) — the folders a user files their threads into, with a colour, a fold, an order, a live "is anything in here working" reading, and an optional rule that files new chats by the folder they start in.

**The auto-file rule matches a folder AND everything inside it, most specific first** (`RunGroupsService.resolveAutoGroupId`). A user who points a group at a project means the project — its `apps/`, its `packages/` — so exact-match-only would silently never fire for the very chats the rule was made for. When two groups both claim a run the longer `autoCwd` wins, which is the only reading under which a group on `~/work` and one on `~/work/app` can usefully coexist; sidebar order breaks a genuine tie, so the answer is deterministic. Containment is SEGMENT-wise (`utils/path-within.ts`) — a plain `startsWith` files every chat opened in `/work/app-old` into the group that claimed `/work/app`. Both sides are canonicalized at the moment they are CHOSEN (`autoCwd` when it is set, `cwd` when the chat is created), so no read path touches the filesystem, and a rule pointing at a folder that does not exist is refused rather than stored: a rule that can never fire is indistinguishable from a feature that does not work. Adding a rule deliberately does NOT sweep up the chats that already exist in that directory — it says where the next one lands, and retroactively moving conversations the user filed by hand would undo their own arrangement without asking.

**Deleting a group releases its chats**; it never takes them with it. The release runs BEFORE the row is dropped, since the reverse order leaves a window in which a run points at a group that no longer exists and a crash inside it makes that permanent. It is the one destructive-looking action in the app that is deliberately NOT behind a confirm dialog — nothing is destroyed — and the menu row says so at the moment the user is deciding (`Delete group · keeps chats`).

**Everything moves by dragging, and the DROP ZONE is the whole section.** A header dragged over another group REORDERS, and the list rearranges under the cursor rather than jumping when the button comes up — the daemon is told once, at `dragend`, because the intermediate arrangements are not decisions. A chat dragged into a section FILES ITSELF there — the only way, since the row's "move to group" menu is gone: filing is one gesture where the menu was a click, a read and a second click. The section — header, rail, rows and the space between them — is one `li` carrying the `dragover`/`drop` pair, because hanging the target off the header alone left the BODY of an expanded group inert: a chat dragged into it lit nothing up and landed nowhere, which is most of what the eye actually aims at. `dragover` bubbles, so one handler covers every child and the rows need none of their own; an empty group has no rows to aim at and is dropped onto through the same section. One piece of drag state carries the KIND for exactly that reason: the same header is a drop target for both gestures and they do different things, which cannot be told apart by guessing from an id. A drop that changes nothing writes nothing. Neither a group nor a chat is draggable while its name is being edited — a text field inside a draggable element cannot be selected with the mouse, because the drag starts instead. The row menus stay (they are the keyboard path, and the only one that can reach `New group…`).

**A group header reads its state from the BADGES its rows are showing, never from the daemon's `status` column** (`runGroupSummary` over `sidebarRunStatus`). The column and the badge disagree in exactly the cases that matter — a live turn under a stale `completed`, a run parked on a question — so a header computed from the row would contradict its own children. `needsInput` is reported ALONGSIDE `busy` rather than folded into it, because a collapsed group would otherwise hide the one state that cannot advance without the user, which is the failure this whole feature would introduce if a header only ever said "something is running".

**A group's chats are enclosed** — indented, hung off a rail in the group's own colour (`GROUP_RAIL_CLASS`) — because without it an expanded group ran straight into the next and the list read as flat. The section's bottom padding is PERMANENT rather than part of the drop highlight: it is what keeps the rail from running flush into the edge below it, and adding it only while a drag hovers would shift every row down at the moment the user is aiming at one. The highlight itself is `muted` and a hairline — `accent` is the palette's warm caramel and read as a coloured state rather than as a destination, and a 2px ring on a block this size in a 260px sidebar reads as an error. An EMPTY group draws no rail (the container exists to enclose rows, and with none it rendered as a 4px orphan tick); its header is the drop target either way. The count sits against the name it counts rather than being pushed to the far right by `flex-1`, where it read as an unrelated second column. Colours are NAMES on the wire and tokens in the renderer (`--group-*`, aliases of the avatar hues so a group dot and an agent avatar cannot drift into two different blues) — a hex could not legally be used at the call site, since the renderer's eslint config makes a colour literal an error.

**A group's options are a PANEL, not a row list** (`Popover`, `anchor="viewport"`). The auto-file rule was a menu row whose label was the folder path and whose action was "open the picker" — so choosing the same folder again looked like nothing happening, and nothing anywhere said what the rule DID. It is now a stated section: a caption in words, the path as a label, and the two things that can be done to it as two small buttons that say which is which (choose another, remove). `anchor="viewport"` measures the trigger and places the panel `fixed`, which is the only way out of the chat list's scroll container — the earlier fix capped the panel's WIDTH to fit inside that clip instead, at 257px in a 235px slot. The colour rows stay a `Menu` on the DOT, because fourteen rows in one panel scrolled its last four — the delete among them — out of sight.

**Deleting a group asks first**, in a dialog that leads with what is NOT deleted ("Its 2 chats are kept — they move to Ungrouped with all their history"). Nothing is destroyed, so the confirm is not there to prevent loss; it is there because *delete* is a word a user reads as final, and a menu row's hint is not where that difference can be stated.

`DaemonSupervisor.start()` reuses a still-healthy daemon left by a prior UI instance (pid + `/health/check` + identity + bundled `version` + the pidfile's `entry` stamp), sweeps stale pidfiles, and only tears down the process it owns. **The `entry` stamp is what makes "is this the current build" answerable at all**: `version` is the package version and is identical across every rebuild, so before it a rebuilt daemon was adopted over and the app went on serving the previous compile — measured as a `pnpm dev` daemon four days old with a whole module missing, which is how instrumentation written to diagnose a bug came never to run. A daemon started from the SAME entry path whose mtime/size have moved is a rebuild and is replaced; one from a DIFFERENT path (`pnpm daemon:dev` on TypeScript source) is a different thing and is left alone; one reporting NO stamp predates the check and is replaced. The other half of that fix is in `turbo.json`, where the `dev` task carries `dependsOn: ["^build"]` — `dist/main.js` is produced by `pnpm build` alone and never by `electron-vite dev`, so without it launching the app ran whatever compile happened to be on disk, and the `@geniro/daemon` dependency in `apps/ui/package.json` (which imports nothing) exists solely to give turbo that ordering edge. **That edge only exists when the launch goes THROUGH turbo**, which is why the root `dev` script is `turbo run dev --filter=@geniro/ui` and must never be "simplified" back to `pnpm --filter @geniro/ui dev`: pnpm runs `electron-vite dev` directly, turbo never enters the picture, and every word above about the ordering silently stops being true. It was `pnpm --filter` for months, and the symptom is exactly the one the `entry` stamp was written to catch and cannot — the stamp only decides whether to REPLACE a running daemon, and a `dist/` nobody rebuilt is stale before any daemon starts. Measured on 2026-08-10: a launch served `apps/daemon/dist/` compiled four days earlier, with no `v1/diagnostics/` directory in it at all, so every `/v1/diagnostics/*` route answered 404 while the source had had them for days — which is why it also passes `GENIRO_IDLE_EXIT_MS` to every daemon it spawns: a daemon it does NOT own (left by a force-quit or a crash) is one it can never stop, so the daemon has to be able to stop itself. The macOS counterpart is on the UI side: `window-all-closed` does not quit on darwin, so a closed window can outlive its daemon's idle window — `app.on('activate')` therefore re-ensures the daemon (`ensureDaemon()`, the one path both launch and reactivation take) before the reopened window can talk to a handle that no longer answers.

### The daemon API client (generated — never hand-mirrored)

The renderer does not restate the daemon's HTTP shapes. `pnpm generate:api`
reads the daemon's own OpenAPI document and emits a typed client into
**`apps/ui/src/renderer/autogenerated/`** (openapi-generator `typescript-fetch`,
mirroring the sibling Geniro web app's `generate:api`). That directory is the
single source of every daemon wire type in the UI — `RunDto`, `ItemDto`,
`Workflow`, `WorkflowNode`, `AgentKind`, `CapabilitiesDto`, … — plus one API
class per swagger tag (`ChatsApi`, `AgentsApi`, `WorkflowsApi`,
`CapabilitiesApi`). It is **committed**; builds and CI never
regenerate. It is excluded from eslint and prettier, and must never be edited by
hand — run the generator instead.

`renderer/daemon-api.ts` is the only hand-written transport left. It owns the
three things a generator cannot know: the loopback base URL (host/port are
negotiated per launch and read from the pidfile), the per-launch bearer token,
and the request timeout + uniform error shape
(`daemon <METHOD> <path> failed (<status>): <detail>`) the UI is written
against. `createDaemonApis(handle)` returns all five clients bound to one
launch.

**Regenerating**: `pnpm generate:api` (root) → `apps/ui/scripts/generate-api.sh`.
It resolves the spec from a throwaway daemon booted out of `apps/daemon/dist`
on a temp userData dir (so it needs nothing running and touches none of the
user's data), or from `GENIRO_SWAGGER_URL` when explicitly pointed at a live
one. It deliberately does NOT read whatever daemon happens to be running: the
generated client is committed, so it must describe the daemon in the WORKING
TREE — a running app can be any older build, and asking it yields a client that
regenerates clean while missing the change. Run it after changing any daemon
route or wire schema, and commit the diff.

**The daemon side of the contract** (`v1/*` modules):
- Wire shapes are **zod schemas** in each module's types file, with the shared
  ones carrying `.meta({ id })` so they land as NAMED OpenAPI components; the
  TS types are `z.infer` of those schemas, so a schema and its type cannot
  drift.
- Responses are declared with **`@ZodResponse({ status, type: SomeDto })`**
  (nestjs-zod v5) — no `@nestjs/swagger` CLI plugin is used or wanted (swc
  cannot run it). One decorator type-checks the handler's return value against
  the schema, serializes the response through it, sets the status code, and
  publishes the schema to the document.
- Every controller carries `@ApiTags` (the generated class name),
  `@ApiBearerAuth`, and `@ApiOperation({ operationId })` (the generated method
  name — descriptive and globally unique; `setupSwagger` throws on duplicates).
- A response DTO's ROOT schema must **not** carry `.meta({ id })`: nestjs-zod
  would then register the component under the id while an array response still
  points at the DTO class name. `setupSwagger` fails the boot on the resulting
  dangling `$ref` rather than letting it degrade a client type to `any`.
- `WorkflowSchema` is deliberately **default-free** so its request and response
  renderings are identical and collapse to one `Workflow` type; YAML leniency
  lives in `WorkflowYamlSchema`, which layers the defaults back on for
  `parseWorkflowYaml` only.
- `McpController` is `@ApiExcludeController()` — a JSON-RPC channel for caller
  agents, not a REST resource the UI client should see.

### Design system (renderer) — tokens + shared components

The renderer is styled with **Tailwind CSS v4 (CSS-first `@theme`)** over a **token layer that is kept in lockstep with the sibling Geniro web app** — same warm cream/caramel palette, same shadcn/ui semantic-token vocabulary — so the desktop app and Geniro web look identical. Its structure is **authoritative**; every new screen builds from it rather than reinventing styles.

```
apps/ui/src/renderer/
  styles/global.css        — the ONLY source of design tokens (:root + @theme inline) + base typography,
                             plus the `.md-editor-surface` retint that maps @uiw/react-md-editor's
                             GitHub-Primer variables onto our tokens (unlayered + double-classed on
                             purpose — the vendor's own light-mode rule is unlayered and loads later).
                             All colours/radii/shadows live here; components never hardcode them.
  components/
    ui/                    — token-driven primitives, shadcn-v4 flavour (data-slot, cva variants):
                             utils.ts (cn), button, input, textarea, label, card, badge,
                             chip (the flat Cursor-style control chip — the ONE source of that
                             look, which select's `ghost` variant is built from),
                             menu + select (the app's ONE dropdown — a custom panel, never a
                             native <select>, whose OS menu ignores every token and cannot
                             render groups/icons/checkmarks/search or be asserted on;
                             its rows are inset rounded pills, a `tone: 'destructive'`
                             row reads red, and `className` caps the panel for a caller
                             whose container is narrower than the default — see the
                             sidebar-group notes above for the measurements),
                             md-editor (markdown + live preview; port of geniro web's md-editor).
    logo, status-dot, field, note-box, error-text,
    error-banner (the dismissible error strip — an ErrorText plus a close
    control and an optional recovery action, for a failure pinned to a screen
    that no input the user can type would ever clear),
    empty-state, collapsible-card,
    copy-button (the app's ONE clipboard control — every code block and any future
    copyable surface; it owns the copied-tick feedback so no caller re-implements it),
    confirm-dialog, expandable-textarea (Textarea + ⤢ → markdown-editor-dialog; use it for any
    prompt-length field), markdown-editor-dialog
                           — app-level shared components composed from the primitives.
  chats/message-bubble.tsx — the transcript-row component (cva variants per item kind).
```

**Hard rules (mechanized in `.claude/rules/renderer-design-system.md` + `.claude/rules/renderer-components.md`, enforced where possible by the eslint override scoped to `apps/ui/src/renderer/**`):**
- **Never hardcode a colour.** Read every colour/radius/shadow from a token in `styles/global.css` — as a utility (`bg-primary`, `text-muted-foreground`, `border-border`, `shadow-panel-sm`) or `var(--token)`. A raw hex/`rgb()`/`hsl()` — including inside a Tailwind arbitrary value like `bg-[#…]` — is an eslint error. Non-colour arbitrary values (`ring-[3px]`, `size-[26px]`, `shadow-[…var(--border)]`) are fine.
- **Never duplicate a component or pattern.** Before adding UI, reach for an existing primitive in `components/ui/` or shared component in `components/`; if a pattern (a button, a field, a status dot, a card, an error line, an empty state) recurs, it lives in a shared component and is imported everywhere — never re-implemented inline.
- **New styled elements go through the layers**: a token in `global.css` → a primitive in `components/ui/` → an app component in `components/`. Compose with the `cn()` helper and, for variants, `cva`. Import directly (`./ui/button`), **no barrels**.
- **Adding a token** (a new colour/shadow) means adding it in `global.css` (`:root` + `@theme inline`) once, then referencing it — never inlining the literal at the call site.
- The palette tracks `geniro/apps/web/src/styles/global.css`; keep token names/values aligned so fixes flow between the repos (same spirit as the vendored `@packages/*`).

### Build toolchain
- **swc** compiles the daemon and all `packages/*` to **CommonJS** (`dist/`), with decorator metadata (`legacyDecorator` + `decoratorMetadata`) — entities and Nest DI rely on it. All share one root `.swcrc` (each build script references it via `--config-file ../../.swcrc`).
- **electron-vite** builds the UI (`out/`).
- Internal `@packages/*` imports resolve to **TypeScript source** via the root tsconfig path alias (`@packages/* → packages/*/src`), so the packages ship **no `.d.ts`**. Type-checking is a separate `tsc --noEmit` (`pnpm check-types`), independent of the swc build.

### Storage split
- **Graph definitions → YAML** (M3) — the source of truth; never stored in SQLite.
- **Settings → `settings.json`** in the Electron userData dir. The composer's chip choices are remembered here (`lastChatTarget`, `lastApprovalMode`, `lastModels` and `lastEfforts` — the last two keyed per CLI, since the two have disjoint model and effort vocabularies — plus `configDir` + `recentConfigDirs`, the optional agent config directory beside `projectFolder`/`recentFolders`) so a new run opens on the choices the user actually works in. `notificationsEnabled` lives here too — ONE switch for both notification kinds, not one per kind: they are the same interruption seen from two sides of a turn, and a user who does not want the app talking to them outside its window does not want half of it. Values whose vocabulary belongs to the DAEMON stay opaque strings on the Electron side (`shared/contracts.ts` holds no daemon shapes); the renderer validates them against the generated enum before they reach a run.
- **MCP toggles → the CLI's own config** — the ONE deliberate exception to "geniro writes only its own files". claude's toggle edits `projects[<cwd>].disabledMcpServers` in `~/.claude.json`, because that is the only mechanism that reaches servers of every scope (probe-verified on 2.1.222; the settings-file `disabledMcpjsonServers` route it replaced could only ever switch off a project `.mcp.json` server, so user- and local-scope rows rendered permanently locked). Sharing the CLI's list is also the point: a switch flipped in geniro is the switch the user sees in their terminal, and the reverse. The write is a read-modify-write under `proper-lockfile` at `~/.claude.json.lock` — the SAME lock the CLI takes — plus tmp+rename, and it REFUSES on an unparseable config rather than replacing a file that holds the user's whole CLI state. geniro's own `<userData>/mcp-settings.json` and the per-turn `--settings` file are gone with it. cursor's half of the exception writes nothing directly: geniro runs that CLI's `mcp enable|disable` in the folder and lets it own the file (`~/.cursor/projects/<key>/mcp-disabled.json`), which is the same sharing with one less thing to get right.
- **Secrets → there are none.** The app stores no credentials: both CLIs authenticate from their own login state (`claude auth login`, `cursor-agent login`), so `keychain.ts`, the save/has/delete secret IPC channels, `SecretName` and the `@napi-rs/keyring` dependency were all removed. `cursor.apiKey` was the only secret this app ever held; since `SecretName` had exactly one member and zod rejects an empty enum, there was no coherent way to keep the surface dormant without a placeholder member no caller could pass. `main/purge-legacy-secret.ts` deletes the entry earlier versions wrote, via the macOS `security` CLI (the keyring library is gone, so the built-in is what remains) — idempotent, failure-swallowing, and carrying a written expiry so a one-release migration does not become permanent. **The rule itself stands**: any future secret goes in the Keychain only — never SQLite, never a config file. A `CURSOR_API_KEY` the USER exports in their own shell is not geniro's to store; it reaches only the cursor child (see *Constraints*).
- **SQLite (`geniro.db`) → runtime/history only** — `runs` / `items` / `node_state` rows.
- **Pasted images → files** under `<userData>/attachments/<runId>/` (`AttachmentStoreService`); only the `{id, mediaType}` row rides the message item's payload, keeping blobs out of the DB. Delivery is per CLI: claude gets real base64 image **content blocks** in its stream-json stdin (probe-verified on 2.1.220 — no `Read` tool, no permission gate; `claude-images.utils.ts`). The ACP path sends the same bytes as ACP `image` content blocks in `session/prompt` (`acp/acp-content.ts`), ahead of the text block — but ONLY to an agent that advertised `promptCapabilities.image`. One that did not gets the text alone plus a `notice` naming what was withheld: the protocol requires the check, an unadvertised image block earns an error reply that would fail the whole turn over an attachment, and dropping it silently would leave the user watching the agent answer about a screenshot it never received.
- **Persist-then-emit** for streamed-then-replayable data (chat `items` and graph-node items alike): allocate the monotonic `seq`, write the row, **then** publish on the RxJS bus / per-run Socket.IO room. SQLite is the source of truth and a reconnecting client replays via an `afterSeq` cursor, so nothing is emitted before it is durable.
- The per-launch loopback **token on disk** (in `daemon.json`) is allowed — it is a local session token, not a user secret.
- **Debug log → 0600 JSONL** under `<userData>/logs/`, rotated, newest few kept (`v1/diagnostics/utils/debug-sink.ts`). It is a file the user can open, copy and paste into a bug report, so **every entry is scrubbed of registered secrets on its way in** (`utils/redact.ts` — the launch token is registered in `main.ts` one statement after it is minted, so there is no window in which it could be written unredacted; a `CURSOR_API_KEY` inherited from the user's own shell is registered on the same line and the same rule, and the per-node MCP call token registers at its own mint site in `CallTokenRegistry.issue()`, which is where a credential minted after boot has to do it). Registration, not pattern-matching: the daemon KNOWS its own secrets because it minted them, while a "looks like a token" regex both misses real ones and mangles innocent hex. With `agent-stdio` on the file also holds the raw CLI conversation — the user's own source — which is why that channel is off unless switched on.

**Two inspectors, one per process.** The renderer is a browser, so Chrome DevTools works on it unchanged — Electron's default menu binds ⌥⌘I, and the debug panel carries a button for it (`GeniroApi.toggleDevTools` → `event.sender.toggleDevTools()`, the SENDER's own WebContents so one window cannot open another's). It covers the DOM, renderer exceptions and every daemon HTTP call and `/ws` frame the renderer makes — which is why no HTTP channel was added to the debug panel. What it can NEVER cover is the daemon or a spawned CLI: separate processes, no page, no renderer network stack. For those the daemon is spawned with `--inspect=127.0.0.1:<DAEMON_INSPECT_PORT>` when the `daemonInspect` setting is on (`DaemonSupervisor.spawnDaemon`, argv AHEAD of the entry script — placed after, node hands it to the daemon as its own argument and it silently does nothing), and `chrome://inspect` attaches real DevTools to it: breakpoints, profiler, heap. Verified under `ELECTRON_RUN_AS_NODE=1 electron`, which the daemon runs as. The port is 9229 because that is the one address `chrome://inspect` discovers with no configuration. It is a SETTING, off by default and restarting the daemon when flipped (an inspector is a launch flag): an open inspector port is code execution inside the daemon for anything that reaches loopback — fine on a single-user machine, not something to hand a packaged install unasked. Note the split in what each shows: the daemon's own log lines do NOT appear in its inspector Console (pino and Nest's `ConsoleLogger` write to fd 1 directly, bypassing the `console` object the inspector forwards) — the log is the debug panel's job, the inspector's is the code.

---

## Coding conventions

- **No `any`** — use specific types, generics, or `unknown` + type guards.
- **All imports at the top** of the file.
- **Naming**: PascalCase for classes/interfaces/enums/types; camelCase for variables/functions.
- **Errors**: throw the custom exceptions from `@packages/common` (e.g. `NotFoundException`, `BadRequestException`). Never swallow errors silently.
- **Shared packages** are aliased as `@packages/*` (e.g. `import { … } from '@packages/common'`), resolving to each package's `src`.
- **Entities** use `@mikro-orm/decorators/legacy` decorators, extend `TimestampsEntity` from `@packages/mikroorm`, and declare **explicit column types** (`@PrimaryKey({ type: 'string' })`, `@Property({ type: 'integer' | 'text' | … })`) — MikroORM's discovery needs them under swc.
- **New daemon feature modules** follow the layered structure as they're added: Controller (route + delegation only — ALL business logic in services, a controller file holds exactly one `@Controller` class with no module-scope functions) → Service (business logic) → DAO (extends `BaseDao`, injects `EntityManager` from `@mikro-orm/sqlite`) → Entity. Use Zod DTOs via `createZodDto()` from `nestjs-zod` for HTTP input. Module-shared types/interfaces are declared in the module's root types file (`<name>.types.ts`), never inline in a service/controller file.
- **Daemon module directory layout** (mechanized in `.claude/rules/daemon-module-structure.md`): a module keeps only `<name>.module.ts` + its types file at the root; every other file lives in its kind-directory — `controllers/`, `services/`, `dao/`, `entity/`, `dto/`, `utils/`, `adapters/`, `gateways/` — with specs co-located. Never a flat module.
- **CLI agent adapters** (mechanized in `.claude/rules/agent-adapters.md`): **every fact about a specific CLI lives in the adapter layer** — as a field of that adapter's `AdapterConfig` or behind an abstract method on `AgentAdapter` (`v1/agents/adapters/agent-adapter.ts`), implemented in that CLI's own `adapters/<name>/` subdirectory with all of its classes/types/specs. Nothing else in the daemon branches on which agent it is talking to: an `if (agent === 'claude')` in a service is a missing config field or a missing method, and a CLI with no answer declares that as a fact with the reason beside it. Consumers compose (WHEN to ask, how to merge/cache/order), adapters know (HOW to ask, where the CLI keeps things, what it supports) — see `ModelsService` / `SkillsService` over `listModels` / `listSkills` / `listReportedCommands`. **Prefer ACP for every new agent**: check whether the CLI ships an Agent Client Protocol server (`cursor-agent acp`, Gemini CLI, Copilot CLI, Goose, Cline, …) and, if it does, compose the existing agent-agnostic `adapters/acp/` client instead of writing a new integration against a proprietary `-p`/stream-json mode. ACP gives real permissions (`session/request_permission`), client-supplied MCP servers (`session/new`), and a published event schema — the three things whose absence on the legacy `cursor-agent -p` path cost a `.cursor/mcp.json` merge lifecycle, a trust probe, and a version-volatile mapper. Fall back to a proprietary headless mode only when ACP is genuinely unavailable, and record that in the adapter's doc block.
- **Renderer UI follows the design system** (see *Design system (renderer)* above; mechanized in `.claude/rules/renderer-design-system.md` + `.claude/rules/renderer-components.md`): colours come from tokens in `styles/global.css` only (never hardcoded), and reusable UI is a shared component in `components/ui/` or `components/` (never duplicated inline). Prefer an existing component; promote a recurring pattern into one.

---

## Testing conventions

- **Vitest**, transformed by **swc** (`vitest.base.ts` — `unplugin-swc` + `tsconfigPaths`). Tests run from source; no build step needed.
- **Unit tests** are co-located as `*.spec.{ts,tsx}` next to the source. Run with `pnpm test:unit`, or target one workspace with `pnpm --filter <name> test:unit`. **Never** call `vitest` directly.
- **Specs are TYPE-CHECKED.** `pnpm check-types` runs `tsc --noEmit -p tsconfig.json` in every workspace, and that project includes `src/**` — specs and `__tests__/` helpers with it. (`tsconfig.build.json`, which excludes both, is for the swc build's file list only; it is never the type gate.) Vitest transpiles through swc and checks nothing, so a spec left out of the gate can assert against a shape the daemon no longer has and stay green indefinitely — which is exactly what had happened: 113 daemon errors accumulated silently, including fixtures missing fields that had been required for releases. Never narrow a workspace's `check-types` back to a spec-excluding project.
- **React component tests** (UI renderer) must put `// @vitest-environment jsdom` on line 1 — the default project environment is `node`. When a `vi.mock(...)` factory closes over module-scope spies, wrap them in `vi.hoisted(() => ({ … }))`.
- **Must-fail policy**: tests never conditionally skip on missing env/services — a missing prerequisite must fail loudly, not `it.skip`.
- **No flaky tests**: nondeterminism is a bug to fix at the source, not retry around. When any pre-existing problem (failing test, broken local step, latent bug) surfaces mid-task, surface it and propose a fix — never silently skip it.
- **No false pins** (mechanized in `.claude/rules/testing.md`): a test whose name or comment claims to pin a behavior must FAIL when that behavior is reverted; assert the real observable, never a proxy the test itself fabricated; and a defensive branch worth writing is worth a test that enters it.

---

## Constraints (local-first & security)

These are hard rules for v1:

- **No cloud / remote / multi-machine code paths.** Everything is local.
- **No Python runtime.** The entire stack — including the CLI-agent layer — is TypeScript.
- **Secrets live in the macOS Keychain only** — never in SQLite, never in a file. (The loopback session token in `daemon.json` is not a user secret and is allowed on disk.) The app currently stores **no secrets at all** — both CLIs carry their own login — so this is policy awaiting its next case, not a description of live code. When the daemon spawns an agent/child process it builds the child env by **stripping every `GENIRO_`-prefixed key** — daemon config travels as `GENIRO_<NAME>` (e.g. `GENIRO_USER_DATA`, `GENIRO_CLAUDE_BIN`) — **plus a named set of credentials it must not hand across agents** (`CURSOR_API_KEY`, the Anthropic keys, `CLAUDE_CONFIG_DIR`; see `utils/child-env.ts`), and **re-injects only what a given child is entitled to**: the Anthropic credentials for the definitionally-claude paths, and a `CURSOR_API_KEY` the user exported in their own shell for the cursor child alone. So no spawned agent inherits another agent's credential or the daemon's internal env. Note the strip is what makes this true, not the absence of a key: the credentials on that list are ones the daemon INHERITED rather than minted, so leaving any of them unstripped would hand the user's own credential to the wrong agent.
- **Every child process the daemon spawns registers with `ProcessRegistry`** (claim → register → auto-unregister on settle) so `OnApplicationShutdown` and explicit cancel terminate it — never spawn an unmanaged child. The M1 shutdown path only removes the pidfile and the UI's `SIGKILL` escalation bypasses Nest hooks, so an unregistered child orphans mid-turn (M3's graph engine spawns N agents — that is where this bites).
- **Every DETACHED child group is also written to the child journal** (`v1/agents/utils/child-journal.ts` → `<userData>/children.json`), and the next boot reaps whatever survived (`services/stranded-child-reaper.service.ts`, called first in `main.ts` before the schema sync so no turn can race it). `ProcessRegistry` is in memory, so it can only serve the GRACEFUL path; a SIGKILL skips Nest's hooks entirely and a `detached` group is not in the daemon's own process group, so it is reparented to launchd and runs until reboot — measured here as two `cursor-agent acp` groups, ppid 1, 14 hours old, 217 MB each. The journal is the only mechanism that survives that. Recording happens inside the spawn helpers themselves (`defaultSpawn` in `utils/spawn-cli.ts`, and `AgentAdapter.runAsProcessGroup`, whose `mcp list` launches the user's own MCP servers) — the pid exists there and nowhere earlier, and a record written later leaves a window where a group is running and unrecorded. The reaper **never kills on a pid alone**: each entry is re-confirmed through `utils/process-identity` first, because a pid recorded hours ago may by then be the user's own interactive `claude`. Unconfirmable entries are left alone — a surviving stray costs memory, a mistaken SIGKILL costs the user's work. A plain `execFile` utility child is deliberately NOT journaled (single pid, node's own timeout, exits in under a second).
- **Graph definitions are YAML** (the source of truth). SQLite holds runtime/history only — never graph definitions.
- **The daemon binds loopback (`127.0.0.1`) only** and gates every non-public route with the per-launch bearer token.
- **No tmux / PTY-scraping for graph execution** in v1 (a click-through PTY mirror for inspection is a later, separate concern).
- **Never use `--no-verify`** when committing.

---

## A note on vendored packages

`packages/{common,http-server,metrics,mikroorm}` are copied from the sibling Geniro repo (`/Users/sergeirazumovskij/Desktop/Projects/Geniro/geniro`) and adapted: Sentry stripped from `common` and the `http-server` exception path; the mikroorm driver swapped Postgres → `@mikro-orm/sqlite`; OIDC auth in `http-server` left dormant. `http-server`'s `runHttpApp` / `buildHttpServerExtension` also gained backward-compatible `host` / `portFallback` / `onListening` options (so the loopback daemon can bind 127.0.0.1, fall back to a free port, and learn the bound port) — the defaults preserve Geniro's `0.0.0.0` listen behavior, so it stays upstreamable. `mikroorm`'s `BaseDao` gained `hardDeleteIncludingSoftDeleted` — a purge that reaches rows the `softDelete` filter hides, added for the chat DELETE route; it is a separate method rather than a flag on `hardDelete` so the destructive form can never be reached by passing an option through. Keep changes minimal and local-first; the goal is to stay close enough to Geniro that fixes can flow between the repos.
