import { Folder, GitBranch, Pencil, Plus, Trash2 } from 'lucide-react';
import * as React from 'react';

import type { CliDetection, CliKind, RunConfig } from '../../shared/contracts';
import { MAX_RUN_CONFIG_NAME, MAX_RUN_CONFIGS } from '../../shared/contracts';
import type { ChatApprovalMode } from '../autogenerated';
import { EmptyState } from '../components/empty-state';
import { ErrorText } from '../components/error-text';
import { SettingRow } from '../components/setting-row';
import { SettingsList, SettingsPanel } from '../components/settings-panel';
import { Button } from '../components/ui/button';
import { Dialog } from '../components/ui/dialog';
import { Input } from '../components/ui/input';
import type { DaemonApis } from '../daemon-api';
import { ApprovalModeSelect } from './approval-mode-select';
import { BranchValueSelect } from './branch-select';
import {
  isChatApprovalMode,
  targetAgentKind,
  workflowSlugOf,
} from './composer-values';
import { ConfigDirSelect } from './config-dir-select';
import { ContextWindowSelect } from './context-window-select';
import { shortenPath } from './directory-select';
import { EffortSelect } from './effort-select';
import { FolderSelect } from './folder-select';
import {
  ModelParameterSelect,
  withModelParameter,
} from './model-parameter-select';
import { ModelSelect } from './model-select';
import type { RunConfigDraft } from './run-config';
import type { TargetWorkflow } from './target-select';
import { TargetSelect } from './target-select';
import { useAgentContextWindows } from './use-agent-context-windows';
import { useAgentEfforts } from './use-agent-efforts';
import { useAgentModelParameters } from './use-agent-model-parameters';
import { useAgentModels } from './use-agent-models';
import { useGitInfo } from './use-git-info';

/**
 * The saved run configurations: pick one to start a chat from, and manage the
 * set.
 *
 * ONE component behind TWO surfaces. As a dialog it hangs off the chat
 * sidebar's `+`, where a configuration is edited in the moment you notice it is
 * wrong; with `asDialog={false}` the same list and editor render inline as the
 * Settings screen's own pane. Two call sites, one implementation — the state
 * they both write is a single `runConfigs` key, so a second copy of this list
 * would be two surfaces free to disagree about the user's data.
 *
 * `onApply` is what separates them, and it is a real difference rather than a
 * flag: the `+` is for USING a configuration, so its rows start a chat; the
 * Settings pane is for KEEPING them, and has no composer to seed — a row there
 * that silently navigated away would lose an edit in progress.
 *
 * The editor COMPOSES the composer's own chips rather than re-implementing
 * pickers, so a configuration can only ever hold values the composer itself
 * could produce. Its vocabulary is asked about the CONFIGURATION's agent, never
 * the composer's — answering the second with the first is the session picker's
 * old bug, which sent a claude profile to cursor.
 */
/** A draft for a composer with no folder yet — the folder chip asks for one. */
const EMPTY_DRAFT: RunConfigDraft = {
  name: '',
  cwd: '',
  branch: null,
  target: 'claude',
  model: null,
  effort: null,
  contextWindow: null,
  modelParameters: {},
  approval: null,
  configDir: null,
};

export function RunConfigPicker({
  open,
  openTo = 'list',
  asDialog = true,
  configs,
  agentsApi,
  workflows,
  cliDetections,
  recentFolders,
  recentConfigDirs,
  approvalModesFor,
  configDirReasonFor,
  planSupported,
  captureCurrent,
  onApply,
  onSave,
  onDelete,
  onClose,
}: {
  open: boolean;
  /**
   * Which surface an open lands on. `new` goes straight to a fresh editor, for
   * the `+` menu's "New configuration…" row — routing that through the list
   * would make the user press New a second time to reach the thing they asked
   * for by name.
   */
  openTo?: 'list' | 'new';
  /**
   * False renders the list and editor INLINE, for the Settings pane. `open` is
   * then the mount itself — the reset effect still keys on it, which is what
   * makes navigating away and back start on the list rather than on a
   * half-filled editor from last time.
   */
  asDialog?: boolean;
  configs: readonly RunConfig[];
  agentsApi: DaemonApis['agents'] | null;
  workflows: readonly TargetWorkflow[];
  cliDetections: readonly CliDetection[] | null;
  recentFolders: string[];
  recentConfigDirs: string[];
  /** Modes one CLI honours, or null until the capability report lands. */
  approvalModesFor: (kind: CliKind) => readonly ChatApprovalMode[] | null;
  /** Why one CLI takes no config directory — null when it can, undefined while unknown. */
  configDirReasonFor: (kind: CliKind) => string | null | undefined;
  planSupported: boolean;
  /**
   * The composer's current setup as a draft, or null when it has no folder yet
   * — there is nothing to save before that.
   */
  captureCurrent: (name: string) => RunConfigDraft | null;
  /**
   * Start a chat from this configuration. Absent on the Settings pane, where
   * there is no composer to seed — the rows there edit instead of launching.
   */
  onApply?: (config: RunConfig) => void;
  /** Create when the id is new, replace when it matches an existing entry. */
  onSave: (draft: RunConfigDraft, id: string | null) => void;
  onDelete: (id: string) => void;
  /** Dismiss the dialog. Unused, and unneeded, when rendered inline. */
  onClose?: () => void;
}): React.JSX.Element {
  /**
   * `null` is the list; anything else is the editor, carrying the id it will
   * save back to (`null` for a configuration that does not exist yet).
   */
  const [editing, setEditing] = React.useState<{
    id: string | null;
    draft: RunConfigDraft;
  } | null>(null);
  /** The row whose Delete has been pressed once — the second press commits. */
  const [confirmingDelete, setConfirmingDelete] = React.useState<string | null>(
    null,
  );
  const [error, setError] = React.useState<string | null>(null);

  // Read through a ref by the open-reset effect below. `captureCurrent` is
  // rebuilt whenever any composer choice moves, and an effect that depended on
  // it would re-run mid-edit — resetting the editor and discarding the draft
  // the user was filling in.
  const captureRef = React.useRef(captureCurrent);
  captureRef.current = captureCurrent;

  // Seeded from the composer so "new configuration" is a naming step rather
  // than six pickers.
  const freshDraft = React.useCallback(
    (): RunConfigDraft => captureRef.current('') ?? EMPTY_DRAFT,
    [],
  );

  // Every open starts on the surface the caller asked for, with nothing
  // half-confirmed: an editor or an armed delete left over from last time is a
  // state the user did not choose and, in the delete's case, one press away
  // from destroying a setup.
  React.useEffect(() => {
    if (open) {
      setEditing(openTo === 'new' ? { id: null, draft: freshDraft() } : null);
      setConfirmingDelete(null);
      setError(null);
    }
  }, [open, openTo, freshDraft]);

  const startNew = React.useCallback((): void => {
    setError(null);
    setEditing({ id: null, draft: freshDraft() });
  }, [freshDraft]);

  const commit = React.useCallback((): void => {
    if (!editing) {
      return;
    }
    const name = editing.draft.name.trim();
    if (name === '') {
      setError('Give the configuration a name.');
      return;
    }
    if (editing.draft.cwd === '') {
      setError('Choose the folder this configuration opens in.');
      return;
    }
    // Refuse here what the IPC schema would refuse anyway. That boundary
    // rejects the WHOLE settings patch by throwing, so without these two guards
    // an over-long name or a 51st entry closes the editor, shows the row from
    // React state, and writes nothing.
    if (name.length > MAX_RUN_CONFIG_NAME) {
      setError(
        `That name is ${name.length} characters — keep it to ${MAX_RUN_CONFIG_NAME}.`,
      );
      return;
    }
    if (editing.id === null && configs.length >= MAX_RUN_CONFIGS) {
      setError(
        `You already have ${MAX_RUN_CONFIGS} configurations — delete one before adding another.`,
      );
      return;
    }
    // Clear the refusal this save just resolved. The message renders above the
    // LIST as well as the editor, so a stale one sits over the row that proves
    // it wrong — the user is told to name a configuration that is on screen
    // carrying its name.
    setError(null);
    onSave({ ...editing.draft, name }, editing.id);
    setEditing(null);
  }, [editing, configs.length, onSave]);

  const body = (
    <>
      {error ? <ErrorText>{error}</ErrorText> : null}
      {editing ? (
        <RunConfigEditor
          draft={editing.draft}
          isNew={editing.id === null}
          agentsApi={agentsApi}
          open={open}
          workflows={workflows}
          cliDetections={cliDetections}
          recentFolders={recentFolders}
          recentConfigDirs={recentConfigDirs}
          approvalModesFor={approvalModesFor}
          configDirReasonFor={configDirReasonFor}
          planSupported={planSupported}
          onChange={(draft) => setEditing({ id: editing.id, draft })}
          onCancel={() => {
            setError(null);
            setEditing(null);
          }}
          onCommit={commit}
        />
      ) : (
        <div className="flex min-h-0 flex-col gap-3">
          {onApply ? (
            <p className="text-sm text-muted-foreground">
              A saved setup — folder, branch, agent and its options. Picking one
              fills in the new-chat screen; nothing is sent until you write a
              message.
            </p>
          ) : null}

          {/* The toolbar leads the list rather than trailing it. Below it, the
              New button sat under whatever the list happened to end with —
              level with a row's Delete on a short list, half a screen down on a
              long one — so the one control that is about the LIST looked like
              it belonged to the last item in it. */}
          <div className="flex items-center justify-between gap-3">
            {/* Silent on an EMPTY list: the empty state below already says
                there is nothing here, in more words and better ones, and the
                two together printed `No configurations yet` twice 80px apart.
                A count is worth stating once there is something to count. */}
            <span className="text-xs text-muted-foreground">
              {configs.length === 0 ? null : `${configs.length} saved`}
            </span>
            <Button
              type="button"
              variant="outline"
              size="sm"
              onClick={startNew}>
              <Plus className="size-4" />
              New configuration
            </Button>
          </div>

          {configs.length === 0 ? (
            // Inside the SAME enclosure the list gets, so the pane does not
            // change shape between having nothing and having one row — an
            // unbordered block floating where a card is about to be reads as a
            // different screen rather than as the same one, empty.
            <SettingsPanel>
              <EmptyState className="flex-col gap-1 py-10">
                <span className="text-foreground">No configurations yet</span>
                {/* `text-xs`, the same step down a settings row puts between
                    its label and its description: the line under the title is
                    a note about it, and at the title's own size the two read
                    as one paragraph in two colours. */}
                <span className="max-w-sm text-xs">
                  A new one starts from the setup you have now — change
                  whichever chips you like, and give it a name.
                </span>
              </EmptyState>
            </SettingsPanel>
          ) : (
            // `overflow-hidden` so a row's hover fill clips to the card's
            // radius — see the twin note in `settings/fast-actions.tsx`.
            <SettingsList className="overflow-hidden">
              {configs.map((config) => (
                <RunConfigRow
                  key={config.id}
                  config={config}
                  confirmingDelete={confirmingDelete === config.id}
                  onApply={onApply}
                  onEdit={() => {
                    setError(null);
                    setConfirmingDelete(null);
                    const { id: _id, ...draft } = config;
                    setEditing({ id: config.id, draft });
                  }}
                  onDeletePress={() => setConfirmingDelete(config.id)}
                  onDeleteCancel={() => setConfirmingDelete(null)}
                  onDeleteConfirm={() => {
                    setConfirmingDelete(null);
                    onDelete(config.id);
                  }}
                />
              ))}
            </SettingsList>
          )}
        </div>
      )}
    </>
  );

  // The pane IS the component when there is no dialog to put it in — Settings
  // renders it inline, and this file stays the ONE implementation of the list
  // and the editor rather than growing a second copy over there.
  if (!asDialog) {
    return body;
  }
  return (
    <Dialog
      open={open}
      // Only reachable on the dialog arm, where the call site passes one; the
      // fallback keeps the prop optional for the inline arm without making
      // `Dialog`'s own contract optional.
      onClose={onClose ?? (() => {})}
      title={
        editing
          ? editing.id === null
            ? 'New configuration'
            : 'Edit configuration'
          : 'Start from a configuration'
      }
      // Wide enough for a row to state its folder path — a leaf name alone does
      // not identify a project among the several checkouts a machine holds.
      className="w-[min(48rem,calc(100vw-3rem))] max-w-none">
      {body}
    </Dialog>
  );
}

/** One saved configuration: what it opens, and the two things you can do to it. */
function RunConfigRow({
  config,
  confirmingDelete,
  onApply,
  onEdit,
  onDeletePress,
  onDeleteCancel,
  onDeleteConfirm,
}: {
  config: RunConfig;
  confirmingDelete: boolean;
  /** Absent on the Settings pane — see {@link RunConfigPicker}'s `onApply`. */
  onApply?: (config: RunConfig) => void;
  onEdit: () => void;
  onDeletePress: () => void;
  onDeleteCancel: () => void;
  onDeleteConfirm: () => void;
}): React.JSX.Element {
  const workflow = workflowSlugOf(config.target);
  return (
    <li className="flex items-center gap-1 pr-2 hover:bg-sidebar-accent">
      <button
        type="button"
        onClick={() => (onApply ? onApply(config) : onEdit())}
        // Named explicitly: the row's own text is a folder path and a model
        // alias read out as one run-on string. The name states what the press
        // DOES, which differs by surface — the two must not share one label
        // that is true on only one of them.
        aria-label={
          onApply
            ? `Start a chat set up as “${config.name}”`
            : `Edit “${config.name}”`
        }
        title={
          onApply
            ? `Start a chat set up as “${config.name}”`
            : `Edit “${config.name}”`
        }
        className="flex min-w-0 flex-1 flex-col items-start gap-0.5 px-4 py-3 text-left outline-none focus-visible:bg-sidebar-accent">
        <span className="w-full truncate text-sm text-foreground">
          {config.name}
        </span>
        <span className="flex w-full items-center gap-1.5 text-xs text-muted-foreground">
          <Folder className="size-3 shrink-0" aria-hidden="true" />
          {/* Elided from the FRONT — CSS truncation eats the tail, which for a
              path is the only end that says which project it is. */}
          <span className="truncate" title={config.cwd}>
            {shortenPath(config.cwd)}
          </span>
          {config.branch === null ? null : (
            <>
              <span aria-hidden="true">·</span>
              <GitBranch className="size-3 shrink-0" aria-hidden="true" />
              <span className="shrink-0 truncate">{config.branch}</span>
            </>
          )}
          <span aria-hidden="true">·</span>
          <span className="shrink-0">{workflow ?? config.target}</span>
          {config.model === null ? null : (
            <>
              <span aria-hidden="true">·</span>
              <span className="shrink-0 truncate">{config.model}</span>
            </>
          )}
        </span>
      </button>
      {confirmingDelete ? (
        // Two presses rather than a nested dialog, which would fight this one
        // for the focus trap. Cancel is not decoration: the confirm button
        // replaces the delete button under the pointer, so a double-click would
        // otherwise land straight on it.
        <>
          <Button
            type="button"
            variant="ghost"
            size="sm"
            className="shrink-0 text-muted-foreground"
            aria-label={`Keep ${config.name}`}
            onClick={onDeleteCancel}>
            Cancel
          </Button>
          <Button
            type="button"
            variant="destructive"
            size="sm"
            className="shrink-0"
            aria-label={`Confirm delete ${config.name}`}
            onClick={onDeleteConfirm}>
            Delete?
          </Button>
        </>
      ) : (
        <>
          <Button
            type="button"
            variant="ghost"
            size="icon"
            className="size-7 shrink-0 text-muted-foreground"
            aria-label={`Edit ${config.name}`}
            title="Edit"
            onClick={onEdit}>
            <Pencil className="size-4" />
          </Button>
          <Button
            type="button"
            variant="ghost"
            size="icon"
            className="size-7 shrink-0 text-muted-foreground"
            aria-label={`Delete ${config.name}`}
            title="Delete"
            onClick={onDeletePress}>
            <Trash2 className="size-4" />
          </Button>
        </>
      )}
    </li>
  );
}

/**
 * The editor, built from the composer's own chips.
 *
 * Its model and effort lists are fetched for the DRAFT's agent and only while
 * the dialog is open — a cursor model probe spawns a real `cursor-agent acp`,
 * so a closed dialog must not be paying for one.
 */
function RunConfigEditor({
  draft,
  isNew,
  open,
  agentsApi,
  workflows,
  cliDetections,
  recentFolders,
  recentConfigDirs,
  approvalModesFor,
  configDirReasonFor,
  planSupported,
  onChange,
  onCancel,
  onCommit,
}: {
  draft: RunConfigDraft;
  isNew: boolean;
  open: boolean;
  agentsApi: DaemonApis['agents'] | null;
  workflows: readonly TargetWorkflow[];
  cliDetections: readonly CliDetection[] | null;
  recentFolders: string[];
  recentConfigDirs: string[];
  approvalModesFor: (kind: CliKind) => readonly ChatApprovalMode[] | null;
  configDirReasonFor: (kind: CliKind) => string | null | undefined;
  planSupported: boolean;
  onChange: (draft: RunConfigDraft) => void;
  onCancel: () => void;
  onCommit: () => void;
}): React.JSX.Element {
  const workflow = workflowSlugOf(draft.target) !== null;
  const kind = targetAgentKind(draft.target);
  // Null while closed, so neither list is fetched for a dialog nobody opened.
  const vocabularyKind = open && !workflow ? kind : null;
  // Every listing below is asked about the CONFIGURATION's own profile, since
  // each is an ACCOUNT fact — a saved configuration naming a profile has to be
  // edited against that account's models and levels, not the composer's.
  const vocabularyConfigDir = open && !workflow ? draft.configDir : null;
  const { models, loading: modelsLoading } = useAgentModels(
    agentsApi,
    vocabularyKind,
    vocabularyConfigDir,
  );
  const efforts = useAgentEfforts(
    agentsApi,
    vocabularyKind,
    null,
    vocabularyConfigDir,
  );
  // Asked WITH the draft's model, unlike the effort listing above: a window
  // size belongs to the model outright, so there is no CLI-wide list to fall
  // back on — asked without one the daemon answers "pick a model first", which
  // is exactly what the chip then says.
  const contextWindows = useAgentContextWindows(
    agentsApi,
    vocabularyKind,
    draft.model,
    vocabularyConfigDir,
  );
  // Asked about the CONFIGURATION's agent and model like its two neighbours —
  // here it decides which rows exist at all, not only what they contain.
  const modelParameters = useAgentModelParameters(
    agentsApi,
    vocabularyKind,
    draft.model,
    vocabularyConfigDir,
  );
  // Branches of the folder the DRAFT points at, not the composer's.
  const git = useGitInfo(draft.cwd === '' ? null : draft.cwd);
  const approvalModes = approvalModesFor(kind);

  const set = React.useCallback(
    (patch: Partial<RunConfigDraft>): void => onChange({ ...draft, ...patch }),
    [draft, onChange],
  );

  const browseFolder = React.useCallback((): void => {
    void window.geniro.pickProjectFolder().then((chosen) => {
      if (chosen) {
        // A new folder is a new repository, so a branch recorded against the
        // old one would name a ref this one may not have.
        onChange({ ...draft, cwd: chosen, branch: null });
      }
    });
  }, [draft, onChange]);

  const browseConfigDir = React.useCallback((): void => {
    void window.geniro.pickProjectFolder().then((chosen) => {
      if (chosen) {
        onChange({ ...draft, configDir: chosen });
      }
    });
  }, [draft, onChange]);

  return (
    <div className="flex min-h-0 flex-col gap-4">
      <label className="flex flex-col gap-1.5">
        <span className="text-sm font-medium">Name</span>
        <Input
          value={draft.name}
          placeholder="Geniro app · main"
          aria-label="Configuration name"
          maxLength={MAX_RUN_CONFIG_NAME}
          onChange={(event) => set({ name: event.target.value })}
        />
      </label>

      <div className="flex flex-col gap-1.5">
        <span className="text-sm font-medium">Opens with</span>
        <div className="flex flex-col divide-y divide-border rounded-md border border-border">
          <SettingRow label="Agent">
            <TargetSelect
              value={draft.target}
              workflows={workflows}
              cliDetections={cliDetections}
              aria-label="Agent or workflow this configuration starts"
              onChange={(target) =>
                // Switching to a workflow drops the CLI-only choices rather than
                // keeping them invisibly: their rows disappear, and a value the
                // user can neither see nor edit would still be saved.
                workflowSlugOf(target) !== null
                  ? set({
                      target,
                      model: null,
                      effort: null,
                      contextWindow: null,
                      modelParameters: {},
                      approval: null,
                      configDir: null,
                    })
                  : set({ target })
              }
            />
          </SettingRow>
          <SettingRow label="Folder">
            <FolderSelect
              folder={draft.cwd === '' ? null : draft.cwd}
              recentFolders={recentFolders}
              onChoose={(cwd) => onChange({ ...draft, cwd, branch: null })}
              onBrowse={browseFolder}
            />
          </SettingRow>
          {git.info.isRepo ? (
            <SettingRow
              label="Branch"
              hint="Switched before the chat starts; refused over a dirty tree, and the rest still applies.">
              <BranchValueSelect
                info={git.info}
                value={draft.branch}
                onChange={(branch) => set({ branch })}
              />
            </SettingRow>
          ) : null}
          {workflow ? null : (
            <>
              {configDirReasonFor(kind) === null ? (
                <SettingRow label="Profile">
                  <ConfigDirSelect
                    configDir={draft.configDir}
                    recentConfigDirs={recentConfigDirs}
                    unavailableReason={configDirReasonFor(kind)}
                    onChange={(configDir) => set({ configDir })}
                    onBrowse={browseConfigDir}
                  />
                </SettingRow>
              ) : null}
              <SettingRow label="Approvals">
                <ApprovalModeSelect
                  supportedModes={approvalModes}
                  value={
                    isChatApprovalMode(draft.approval) &&
                    approvalModes?.includes(draft.approval)
                      ? draft.approval
                      : null
                  }
                  planSupported={planSupported}
                  onChange={(approval) => set({ approval })}
                />
              </SettingRow>
              <SettingRow label="Model">
                <ModelSelect
                  agentKind={kind}
                  models={models}
                  loading={modelsLoading}
                  value={draft.model}
                  // The window and the other parameters belong to the model
                  // that offered them, so a model change clears both rather
                  // than carrying values the new model may not have — the same
                  // rule the daemon's settings patch holds for an open chat.
                  onChange={(model) =>
                    set({ model, contextWindow: null, modelParameters: {} })
                  }
                />
              </SettingRow>
              {/* The ROW goes with the control, here and below. Both chips
                  render nothing when the model offers no choice, and a labelled
                  row around nothing is a label with a hole in it — which in a
                  fixed label column is louder than the chip ever was. */}
              {efforts.efforts.length === 0 ? null : (
                <SettingRow label="Effort">
                  <EffortSelect
                    efforts={efforts.efforts}
                    value={draft.effort}
                    onChange={(effort) => set({ effort })}
                  />
                </SettingRow>
              )}
              {contextWindows.windows.length === 0 ? null : (
                <SettingRow label="Context window">
                  <ContextWindowSelect
                    windows={contextWindows.windows}
                    value={draft.contextWindow}
                    onChange={(contextWindow) => set({ contextWindow })}
                  />
                </SettingRow>
              )}
              {/* One labelled row per OTHER setting this model enumerates. A row
                  per parameter rather than one row of chips, because the
                  editor's whole shape is a fixed label column — a setting with
                  no address of its own is the wrapping chip row this screen was
                  built to replace. */}
              {modelParameters.parameters.map((parameter) => (
                <SettingRow key={parameter.id} label={parameter.label}>
                  <ModelParameterSelect
                    parameter={parameter}
                    // The row's label column already names it.
                    showAxisName={false}
                    value={draft.modelParameters[parameter.id] ?? null}
                    onChange={(next) =>
                      set({
                        modelParameters: withModelParameter(
                          draft.modelParameters,
                          parameter.id,
                          next,
                        ),
                      })
                    }
                  />
                </SettingRow>
              ))}
            </>
          )}
        </div>
        <p className="text-xs text-muted-foreground">
          A row left on its default means “let the CLI decide”.
        </p>
      </div>

      <div className="flex items-center justify-end gap-2 border-t border-border pt-3">
        <Button type="button" variant="ghost" size="sm" onClick={onCancel}>
          Cancel
        </Button>
        <Button type="button" size="sm" onClick={onCommit}>
          {isNew ? 'Create' : 'Save'}
        </Button>
      </div>
    </div>
  );
}
