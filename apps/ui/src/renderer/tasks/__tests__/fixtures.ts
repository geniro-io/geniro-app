import {
  type ProjectDto,
  type TaskDto,
  TaskPriority,
  TaskSource,
  TaskStatus,
} from '../../autogenerated';

/**
 * Complete `TaskDto` / `ProjectDto` values for the board's specs.
 *
 * Built as real DTOs with no cast, which is the whole point: the specs used to
 * write `({ id, title, status }) as TaskDto` and that cast silenced the type
 * gate on every field it omitted — including `source`, which they set to
 * `'manual'`, a value `TaskSource` does not contain and the daemon cannot
 * emit. A fixture that cannot be constructed is a fixture that has drifted.
 */
export function aTask(over: Partial<TaskDto> = {}): TaskDto {
  return {
    id: 't1',
    projectId: 'p1',
    title: 'Card',
    description: null,
    status: TaskStatus.Todo,
    labels: [],
    source: TaskSource.Geniro,
    sourceRef: null,
    folder: null,
    // The card's own run configuration — null throughout means it inherits the
    // project's, which is the ordinary state of a card nobody has configured.
    agentKind: null,
    model: null,
    effort: null,
    approval: null,
    configDir: null,
    workflowSlug: null,
    branch: null,
    worktreePath: null,
    runId: null,
    reportItemId: null,
    // What the card's run opened — empty for a card nothing has run, which is
    // every fixture that does not say otherwise.
    pullRequests: [],
    number: null,
    attachments: [],
    position: 0,
    priority: TaskPriority.None,
    dueDate: null,
    createdAt: '2026-01-01T00:00:00.000Z',
    updatedAt: '2026-01-01T00:00:00.000Z',
    ...over,
  };
}

export function aProject(over: Partial<ProjectDto> = {}): ProjectDto {
  return {
    id: 'p1',
    name: 'One',
    taskKey: 'ONE',
    folder: '/w',
    groupId: null,
    agentKind: null,
    model: null,
    effort: null,
    approval: null,
    configDir: null,
    workflowSlug: null,
    autopilotEnabled: false,
    autopilotIntakeStatus: TaskStatus.Todo,
    autopilotMaxConcurrent: 1,
    autopilotFailureStreak: 0,
    provider: TaskSource.Geniro,
    createdAt: '2026-01-01T00:00:00.000Z',
    updatedAt: '2026-01-01T00:00:00.000Z',
    ...over,
  };
}
